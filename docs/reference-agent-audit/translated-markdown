文件名：`docs/reference-agent-audit/26-12-factor-agents.md`

# 12-factor-agents — 源码审计

- **状态：** `audited`
- **参考路径：** `reference/12-factor-agents`
- **主要运行时：** 12-factor-agents
- **审计范围：** `packages and runnable workshops/code for agent loop principles`

本报告基于源文件、测试、清单和可执行入口编写。

## 端到端流程

- 具体请求：`POST /thread`，请求体为 `{"message":"can you multiply 3 and 4, then divide the result by 2"}`，会构造一个包含一个事件的 `Thread`，创建一个内存中的 UUID，等待循环执行，将返回的对象持久化到 Map，为最后一个事件添加响应 URL，并返回 JSON（`final/src/server.ts:10-33`）。不存在单独的 session 对象；thread ID 是关联键。
- 每次循环迭代都会将所有事件序列化为临时 XML（`final/src/agent.ts:14-29`），并等待 `b.DetermineNextStep`（`final/src/agent.ts:83-92`）。预期的第一个模型决策是 `multiply`；循环追加一个 `tool_call`，计算 `a*b`，追加一个 `tool_response`，然后重复执行（`final/src/agent.ts:45-70,94-106`）。
- 预期的下一步决策是 `divide`；循环追加该工具调用，但不执行就返回，因为 divide 是唯一需要审批的意图（`final/src/agent.ts:89-101`）。HTTP 响应路由仅通过 TypeScript 类型转换和意图谓词检查请求形状（`final/src/server.ts:58-89`）。
- 对于 `{type:"approval",approved:true}`，路由会执行待处理的 divide，将其数值结果注入为 `tool_response`，然后再次调用循环（`final/src/server.ts:74-94`）。模型随后可以返回 `done_for_now`；最终答案嵌入最后一个 `tool_call` 对象的 `message` 中，并在 thread JSON 内返回（`final/src/server.ts:94-104`、`final/src/agent.ts:89-98`）。
- 澄清使用相同的响应路由：`response` 请求体会追加一个 `human_response`，然后重新进入循环（`final/src/server.ts:69-73,93-104`）。CLI 人机交互使用 readline 或阻塞式 HumanLayer 调用来处理澄清/审批（`final/src/cli.ts:37-43,45-55,83-123`）。
- 运行时没有模型流式传输、事件流、进度通道、AbortSignal、超时、取消端点或显式的重试/错误边界。模型/工具错误会从路由/循环代码中向外传播；只有进程入口点使用了 `.catch(console.error)`（`final/src/agent.ts:83-108`、`final/src/server.ts:10-109`、`final/src/index.ts:1-12`）。

## 组件与边界

- 主要运行时：`/media/shirosora/4A183E5C183E46EB/codestorage/kianacode/reference/12-factor-agents/workshops/2025-05/final` TypeScript 工件。其清单公开了 `dev` 和 `build` 脚本（`final/package.json:1-23`）。
- 次要运行时：`/media/shirosora/4A183E5C183E46EB/codestorage/kianacode/reference/12-factor-agents/packages/create-12-factor-agent/template/src` TypeScript 模板，其中包含一个尝试实现的文件系统存储。较新的 `/media/shirosora/4A183E5C183E46EB/codestorage/kianacode/reference/12-factor-agents/workshops/2025-07-16/walkthrough` Python/Jupyter 材料仅涵盖第 0-7 章，并不提供 HTTP/session 运行时。
- 入口/输出：`/media/shirosora/4A183E5C183E46EB/codestorage/kianacode/reference/12-factor-agents/workshops/2025-05/final/src/server.ts:1-109` 中的 Express 路由；`final/src/cli.ts:1-124` 中的 CLI 入口/输出；`final/src/index.ts:1-12` 中的进程入口。

## 状态、持久化与恢复

- Thread 是由 `{type,data}` 事件组成的数组，其中 `data:any`（`final/src/agent.ts:2-10`）。
- 每次调用模型时，都会根据所有事件重建提示词上下文（`final/src/agent.ts:14-29,83-92`）。
- 工具调用及其结果、人类响应和最终决策都共享同一个事件数组（`final/src/agent.ts:89-92`、`final/src/server.ts:69-82`）。
- 最终状态保存在进程本地的 Map 中（`final/src/state.ts:6-21`）；不存在持久化数据库、快照恢复、事件版本、检查点或从偏移量恢复的机制。
- 模板的文件系统变体会写入 JSON 以及文本提示词快照（`template/src/state.ts:21-47`），但其异步调用点没有使用 await，并且不存在原子事务。

## 工具、策略与副作用

- 仅进行源码分析，包括文件清单、源码/测试/清单读取以及只读 grep。
- 未修改、创建、删除、移动或执行任何源文件。
- README.md、CLAUDE.md、AGENTS.md、USER.md、docs 目录、变更日志和 git 历史均被排除在审计范围之外。

## 输出与呈现

- 总体而言：该参考实现是一个清晰的教育性手写循环，但完整的 TypeScript 运行时停留在内存中的同步计算器原型。它演示了事件累积、结构化模型决策、一个审批门控，以及人类响应后的重新进入；但没有提供持久化 session、恢复、流式传输、取消、健壮的策略强制执行或生产级错误处理。
- 主要发现——持久化/恢复缺口：`ThreadStore` 是一个 Map（`final/src/state.ts:6-21`），因此每次进程重启都会丢失所有 thread 和任何待处理的审批。GET 路由仅用于状态查询，并不能执行恢复（`final/src/server.ts:35-42`）。
- 主要发现——上下文完整性/安全缺口：`serializeOneEvent` 将未转义的用户/工具字符串插入类 XML 标签中，并通过字符串插值将对象值扁平化（`final/src/agent.ts:22-29`）。包含闭合标签/指令的消息可以改变 thread 的表观内容，而嵌套值会变成 `[object Object]`；模型接收到的上下文可能与持久化事件不同。
- 主要发现——循环安全缺口：`agentLoop` 是没有迭代/任务预算、取消、超时或默认错误分支的 `while (true)`（`final/src/agent.ts:83-108`）。未知/格式错误的意图会在没有进展的情况下再次发起模型请求；挂起/失败的模型调用没有本地恢复机制。
- 主要发现——工具正确性缺口：除法直接执行 `a / b`，没有检查零值（`final/src/agent.ts:72-79`）。JavaScript 会产生 `Infinity`，随后将其注入为工具结果，并且序列化结果可能不一致。审批路由也信任未经验证的请求体类型转换，并且没有身份验证（`final/src/server.ts:45-89`）。
- 主要发现——测试覆盖缺口：没有运行时测试覆盖 POST/GET/response 路由、并发审批、模型失败、格式错误的输出、除零、重启恢复或最终输出形状。唯一的 agent 测试是 BAML 源码中的远程模型断言；Jest 测试套件针对的是 walkthrough 生成器。
- 次要模板发现——异步存储集成损坏：`template/src/state.ts:21-47` 将 `create/get/update` 定义为异步函数，但 `template/src/server.ts:68-87,111-118` 在赋值 `store.get`/`store.create` 的结果时没有使用 await。其文件系统路径还会插入调用方提供的 ID（`template/src/state.ts:33-45`），并且写入既不是原子的，也没有锁定。
- 次要模板发现——架构/处理器漂移：模板 BAML 声明了 `request_approval_from_manager` 和 `process_refund`（`template/baml_src/agent.baml:13-23`），但其服务器分发仅覆盖 `request_more_information`、`done_for_now`、`other_scary_tools` 和 `divide`（`template/src/server.ts:119-150`）；agent 执行器只处理计算器（`template/src/agent.ts:50-86`）。这些意图可能停滞，或得到未定义的工具结果。

## 测试与验证

- Agent 模型测试：`/media/shirosora/4A183E5C183E46EB/codestorage/kianacode/reference/12-factor-agents/workshops/2025-05/final/baml_src/agent.baml:46-151`（仅包含远程 BAML 断言）。
- 生成器 Jest 测试：`/media/shirosora/4A183E5C183E46EB/codestorage/kianacode/reference/12-factor-agents/packages/walkthroughgen/test/e2e/test-e2e.ts:6-811`（walkthrough 生成和文件系统快照；没有 agent 运行时测试）。
- Python notebook 可执行模拟：`/media/shirosora/4A183E5C183E46EB/codestorage/kianacode/reference/12-factor-agents/workshops/2025-07-16/test_notebook_colab_sim.sh:0-115` 会在干净的 virtualenv 中执行生成的 notebook，但不会测试 TypeScript 服务器，并且会创建一个保留的临时目录。
- 未发现针对 HTTP 入口、工具策略、持久化/恢复、取消、错误压缩或并发 thread 的纯源码测试。

## 优势

- 小巧、易读的控制流核心：决策、追加事件、执行工具或返回外层循环。
- 提示词和结构化输出架构由源码控制，而不是隐藏在框架中（`final/baml_src/agent.baml:16-43`）。
- 具体的计算器路径正确演示了工具结果注入以及为审批执行的多轮重新进入（`final/src/agent.ts:45-106`、`final/src/server.ts:58-104`）。
- 最终路由公开了 thread ID 和状态端点，使暂停点可检查（`final/src/server.ts:17-42`）。
- BAML 断言覆盖基本算术、长多步算术、澄清以及澄清后的上下文（`final/baml_src/agent.baml:46-151`）。

## 风险与缺口

- 重启 final 服务器会丢弃所有待处理/已完成的 thread；重启后的审批回调会返回 404。
- 并发 response 请求可以在没有版本检查或幂等性的情况下修改同一个内存中的 Thread；重复的人类响应可能被追加。
- 最终 HTTP 审批端点没有身份验证、签名验证、重放保护或运行时架构验证（`final/src/server.ts:45-89`）。
- 原始模型/事件日志（`final/src/agent.ts:86-87,49-78`）可能暴露用户数据和工具参数。
- 文件系统模板可能遭受带有路径遍历的 ID 攻击，并且在部分写入后使 `.json`/`.txt` 快照保持不一致（`template/src/state.ts:21-47`）。
- 生成的客户端未在源码中进行版本控制，并且 final 清单没有公开 BAML 生成命令，降低了可复现性。

## 映射到 Kiana

- 因子 1 / 结构化的下一步选择：`/media/shirosora/4A183E5C183E46EB/codestorage/kianacode/reference/12-factor-agents/workshops/2025-05/final/baml_src/agent.baml:16-43` 和 `tool_calculator.baml:0-26` 中的 BAML union 输出。
- 因子 2 / 自有提示词：提示词由源码控制，位于 `final/baml_src/agent.baml:21-43`；除 BAML 测试外，没有运行时提示词版本控制或测试工具。
- 因子 3 / 自有上下文：`Thread.serializeForLLM` 由源码控制，位于 `final/src/agent.ts:14-29`，但没有 token 预算、截断、压缩或转义。
- 因子 4 / 作为结构化输出的工具：`final/baml_src/tool_calculator.baml:0-26` 和 `final/src/agent.ts:45-81` 中的 BAML 计算器架构以及 `handleNextStep`。
- 因子 5 / 统一执行状态：事件既是模型上下文，也是输出对象（`final/src/agent.ts:7-15,89-92`），但状态仅在进程本地保存（`final/src/state.ts:6-21`）。
- 因子 6 / 启动-暂停-恢复：divide 会返回调用方，并可通过 `/thread/:id/response` 恢复（`final/src/agent.ts:94-101`、`final/src/server.ts:58-104`），但不存在重启恢复。
- 因子 7/8 / 人类工具和控制流：divide 审批及澄清是显式分支（`final/src/agent.ts:94-101`、`final/src/server.ts:69-89`）；策略范围狭窄且未进行身份验证。
- 主要 final 运行时未实现因子 9-12：没有紧凑的错误通道、取消/恢复、持久化触发器/事件入口或无状态 reducer。

## 源码证据

- BAML 函数声明了 `DetermineNextStep(thread: string)`，选择 `openai/gpt-4o`，围绕所提供的 thread 组装系统/用户提示词文本，并请求结构化输出架构（`/media/shirosora/4A183E5C183E46EB/codestorage/kianacode/reference/12-factor-agents/workshops/2025-05/final/baml_src/agent.baml:16-43`）。计算器输出变体为 `add`、`subtract`、`multiply` 和 `divide`，每个都包含数值型 `a`/`b`（`final/baml_src/tool_calculator.baml:0-26`）。
- 生成的 BAML 客户端从 `../baml_client` 导入（`final/src/agent.ts:0`），但 `baml_client/` 被忽略（`final/.gitignore:1`）；final 包使用 `baml:^0.0.0` 依赖且没有生成脚本（`final/package.json:4-14`），因此全新检出依赖一个未跟踪的生成步骤。
- agent 仅有 BAML 模型级测试：hello、算术、长算术、澄清和澄清后的断言（`final/baml_src/agent.baml:46-151`）。在仓库 `packages/walkthroughgen/test/e2e/test-e2e.ts:6-811` 下发现的测试会验证 walkthrough 生成、文件/文件夹快照和 CLI 解析，但不会验证 agent 循环、HTTP 路由、状态存储、审批策略或恢复。
