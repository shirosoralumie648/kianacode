---
phase: 01-baseline-evidence-governance
plan: "06"
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
  - 01-05
files_modified:
  - scripts/capability_governance.py
  - scripts/validate-capability-governance.py
  - scripts/capability-governance-smoke.sh
autonomous: true
requirements:
  - COD-01
  - DIF-11
must_haves:
  truths:
    - "D-06/D-12/D-17/D-18: one standard-library module owns strict journey aggregation, governance/product separation, and a deterministic current-matching-evidence predicate that rejects expired proof and older success followed by a newer failed retest."
    - "D-01/D-02/D-09..D-11/D-21: official-source coverage, exact aliases, cross-family references, fingerprints, and mandatory ancestry fail closed with deterministic codes."
    - "D-13..D-17/D-24: proof, freshness, evidence history, bounded paths, and drift use one semantic implementation behind a thin offline CLI."
  artifacts:
    - path: scripts/capability_governance.py
      provides: Shared semantic, ancestry, fingerprint, and rendering-safe model
      exports:
        - PROOF_RANK
        - counts_as_complete
        - validate_governance
        - validate_revision_ancestry
        - detect_drift
    - path: scripts/validate-capability-governance.py
      provides: Thin validate/history/drift diagnostic CLI
    - path: scripts/capability-governance-smoke.sh
      provides: Progressive public-baseline, reference-governance, and semantic-negative slices
  key_links:
    - from: scripts/validate-capability-governance.py
      to: scripts/capability_governance.py
      via: imports all semantic operations without duplicated rules
      pattern: from capability_governance import
    - from: scripts/capability_governance.py
      to: scripts/validate-json-schema.py
      via: structural validation precedes semantic graph validation
      pattern: validate-json-schema.py
    - from: scripts/capability-governance-smoke.sh
      to: scripts/validate-capability-governance.py
      via: named semantic slices invoke the shared CLI and assert exact status/error codes
      pattern: semantic-negative
---

<objective>
实现 Phase 1 唯一 semantic truth gate，并以完整 ancestry/source/alias/drift oracle 让所有正负 fixtures 转绿。

Purpose: 将 D-01..D-21 的 completion、identity、history 和 freshness 规则从 schemas/Markdown 收回到一个标准库模块。
Output: 共享 Python module、薄 validator CLI，以及 runner 中三个基于该 CLI 的渐进 semantic slices；不新增 package 或 Rust adapter。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-baseline-evidence-governance/01-03-SUMMARY.md
@.planning/phases/01-baseline-evidence-governance/01-04-SUMMARY.md
@.planning/phases/01-baseline-evidence-governance/01-05-SUMMARY.md
@scripts/validate-json-schema.py

<interfaces>
CLI subcommands and flags are exact: `validate --fixture-bundle PATH --json`, `validate --manifest PATH [--live-reference-root PATH] [--target-root PATH] [--json]`, `validate-history --head PATH --json`, and `check-drift --manifest PATH --live-reference-root PATH --target-root PATH --official-source-artifact PATH --json`.
Exit status is 0 valid/current, 1 structural/semantic/drift failure, 2 usage/read failure. JSON is `{schema,status,errors:[{code,subject_id,path,detail,freshness}]}` sorted by `(code,subject_id,path)`; `freshness` is `stale` for `evidence_expired` and `newer_failed_retest` and null for unrelated errors.
Fixture bundles and production manifests provide explicit RFC3339 `evaluation_time`. Every expiry decision consumes that value; no validator path calls the wall clock to decide current completion.
This plan extends the existing runner with exactly `public-baseline`, `reference-governance`, and `semantic-negative`; it leaves `schemas` and `fixture-shapes` unchanged. Plan 01-07 adds `drift-refresh`, Plan 01-10 adds `legacy-authority`, and Plan 01-11 adds `generated-views` and `production`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bounded loading, source coverage, identities, and completion</name>
  <files>scripts/capability_governance.py, scripts/validate-capability-governance.py</files>
  <read_first>scripts/validate-json-schema.py, scripts/fixtures/capability-governance/valid/minimal-graph.json, scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json, .planning/phases/01-baseline-evidence-governance/01-PATTERNS.md</read_first>
  <behavior>
    - Valid small and 38-reference bundles pass; all four source-coverage fixtures fail with their exact code.
    - Required journeys aggregate with `all`; coverage, freshness, proof rank, and current subject/source/target/environment evidence must all match.
    - `current_matching_evidence` uses bundle/manifest `evaluation_time`; an otherwise-valid success with `expires_at &lt; evaluation_time` returns `evidence_expired`, the exact subject, `freshness=stale`, and cannot count.
    - For one subject/source/target/environment tuple, the highest validated evidence sequence is authoritative; a newer failed result returns `newer_failed_retest`, keeps the older success in history, marks the subject stale, and cannot count.
    - Registry identities reconcile unique IDs/paths and exact aliases/domains; every inventory capability has one complete current decision.
  </behavior>
  <action>Implement `GovernanceError`, bounded UTF-8/JSON loading, repo-relative containment, unique indexing, source-artifact hashing, `validate_official_source_coverage`, registry reconciliation, alias validation, decision validation, `PROOF_RANK`, `current_matching_evidence`, `counts_as_complete`, and strict journey aggregation. Parse the closed bundle/manifest RFC3339 `evaluation_time` once and pass it into every completion/evidence evaluation; never derive expiry state from `datetime.now`, filesystem mtime, or render time. For each exact subject/source/target/environment tuple, validate contiguous immutable sequence, retain every historical record, and evaluate the highest sequence: a failed latest record invalidates older success with `newer_failed_retest`; a successful latest record whose optional `expires_at` is before `evaluation_time` invalidates with `evidence_expired`. Both errors carry the exact subject and `freshness=stale`; neither deletes or rewrites evidence. Invoke the existing structural helper for each extracted family before semantics. Compare artifact entries bidirectionally with the public source index and emit the four Plan 01-04 codes exactly. Validate real `git_head`, normalized tree, license hash, content-only revision, and target revision fields without treating Adopt/Adapt or governance totals as product proof. The thin CLI implements fixture-bundle validation now and imports every operation from the shared module.</action>
  <verify>
    <automated>python3 scripts/validate-capability-governance.py validate --fixture-bundle scripts/fixtures/capability-governance/valid/minimal-graph.json --json &gt;/tmp/kiana-gov-valid.json &amp;&amp; python3 -c 'import json; d=json.load(open("/tmp/kiana-gov-valid.json")); assert d["status"]=="valid"' &amp;&amp; python3 -c 'import json,subprocess,pathlib; p=pathlib.Path("scripts/fixtures/capability-governance/invalid/coverage"); m=json.loads((p/"expected-errors.json").read_text()); [(lambda r,c: (_ for _ in ()).throw(AssertionError((c,r.stdout,r.stderr))) if r.returncode!=1 or c not in r.stdout+r.stderr else None)(subprocess.run(["python3","scripts/validate-capability-governance.py","validate","--fixture-bundle",str(p/x["fixture"]),"--json"],text=True,capture_output=True),x["expected_code"]) for x in m["cases"]]' &amp;&amp; python3 -c 'import json,subprocess,pathlib; m=json.loads(pathlib.Path("scripts/fixtures/capability-governance/invalid/integrity/expected-errors.json").read_text()); selected=[x for x in m["cases"] if x["expected_code"] in {"evidence_expired","newer_failed_retest"}]; assert len(selected)==2; runs=[(x,subprocess.run(["python3","scripts/validate-capability-governance.py",x["command"],*x["args"],"--json"],text=True,capture_output=True)) for x in selected]; docs=[(x,r,json.loads(r.stdout)) for x,r in runs]; assert all(r.returncode==1 and d["status"]==x["expected_status"] and any(e["code"]==x["expected_code"] and e["subject_id"]==x["subject_id"] and e["freshness"]==x["expected_freshness"] for e in d["errors"]) for x,r,d in docs)'</automated>
  </verify>
  <acceptance_criteria>Positive bundles return 0; source corpus returns 1 with exact codes; expiry and newer-failed-retest each return the exact subject, invalid status, and stale freshness under fixture evaluation time; one shared completion predicate controls journey/report semantics; aliases/domains use the frozen exact shape.</acceptance_criteria>
  <done>COD-01 source completeness, DIF-11 identity/decision completeness, and D-17 current-evidence expiry/retest rules are executable, deterministic, and not inferred from counts, prose, or wall time.</done>
  <commit>git add -- scripts/capability_governance.py scripts/validate-capability-governance.py &amp;&amp; git commit -m "feat(01-06): implement governance semantics"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce full ancestry and fingerprint-driven staleness</name>
  <files>scripts/capability_governance.py, scripts/validate-capability-governance.py</files>
  <read_first>scripts/fixtures/capability-governance/invalid/integrity/ancestry-mutations.json, scripts/fixtures/capability-governance/invalid/drift/repository-drift.json, scripts/fixtures/capability-governance/invalid/drift/official-source-drift.json, scripts/fixtures/capability-governance/invalid/drift/target-revision-drift.json</read_first>
  <behavior>
    - Every family head traverses to a genesis revision and rehashes each predecessor path before accepting current state.
    - Removal, reorder, rewrite, missing ancestry, or predecessor hash mismatch fails with its exact code.
    - Git HEAD/tree/license, content-only tree, controlled official artifact, Kiana target drift, and unavailable source observations each mark affected subjects non-current and require a new immutable successor/event or blocked observation.
  </behavior>
  <action>Add `canonical_sha256`, `load_revision_by_binding`, `validate_revision_ancestry`, `validate_evidence_prefix`, `fingerprint_reference`, `fingerprint_target`, and `detect_drift`. Resolve predecessor paths beneath allowed roots, rehash bytes, verify revision IDs, walk until explicit genesis, detect cycles, and compare every immutable record prefix; never accept an optional caller-supplied previous index. Drift comparison uses live repository Git HEAD plus normalized tree/license hashes, content-only normalized tree hash, controlled official artifact bytes/hash, and target Git/artifact fingerprints. Emit the exact Plan 01-05 codes plus `content_tree_drift` and `source_unavailable`; unavailable observations must never be represented as current. Produce structured stale/supersession or blocked-observation proposals without modifying canonical data.</action>
  <verify>
    <automated>python3 -c 'import json,subprocess,pathlib; root=pathlib.Path("scripts/fixtures/capability-governance/invalid"); m=json.loads((root/"integrity/expected-errors.json").read_text()); selected=[x for x in m["cases"] if x["expected_code"].startswith(("history_","previous_revision","genesis_","repository_","license_","official_source_","target_revision_"))]; assert selected; [(lambda r,c: (_ for _ in ()).throw(AssertionError((c,r.stdout,r.stderr))) if r.returncode!=1 or c not in r.stdout+r.stderr else None)(subprocess.run(["python3","scripts/validate-capability-governance.py",x["command"],*x["args"],"--json"],text=True,capture_output=True),x["expected_code"]) for x in selected]'</automated>
  </verify>
  <acceptance_criteria>All ancestry and drift cases fail for their own code; unchanged full chains pass; diagnostics name affected revision/subject without absolute paths or secrets.</acceptance_criteria>
  <done>D-11/D-16/D-17 history and freshness cannot remain current after any required fingerprint changes.</done>
  <commit>git add -- scripts/capability_governance.py scripts/validate-capability-governance.py &amp;&amp; git commit -m "feat(01-06): enforce ancestry and drift"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close semantic/security diagnostics and production manifest validation</name>
  <files>scripts/capability_governance.py, scripts/validate-capability-governance.py, scripts/capability-governance-smoke.sh</files>
  <read_first>scripts/fixtures/capability-governance/invalid/integrity/semantic-mutations.json, scripts/fixtures/capability-governance/invalid/integrity/security-mutations.json, scripts/fixtures/capability-governance/invalid/integrity/alias-contract.json, docs/schemas/kiana-capability-governance-bundle.v1.schema.json</read_first>
  <behavior>
    - All integrity manifest cases fail with exact sorted codes and no generic fallback.
    - `semantic-negative` independently asserts `evidence_expired` and `newer_failed_retest` with their declared subject, `status=invalid`, and `freshness=stale`; neither case is satisfied by the other's mutation.
    - Synthetic oversized and symlink cases are created under temp roots and fail before open/hash escapes.
    - Manifest validation rehashes selected source/family/legacy heads, rejects undeclared fields, and never selects a newest filename.
    - Runner slices `public-baseline`, `reference-governance`, and `semantic-negative` invoke the shared CLI, preserve the earlier task-level direct checks, and assert exact success/failure contracts.
  </behavior>
  <action>Complete `validate_governance`, manifest loading, semantic mutation application, deterministic diagnostics, safe Markdown-input validation, redaction, bounded record counts, and CLI status handling. Require and parse manifest `evaluation_time`, propagate it to `current_matching_evidence`, and prohibit wall-clock fallback. The manifest is otherwise a path/hash selector only; reject completion/proof/count fields and require explicit head paths. Generate oversized/symlink test inputs under temporary directories from mutation instructions. Extend the existing runner without rewriting its two earlier slices: `public-baseline` validates the positive public/source graph and all four exact coverage failures; `reference-governance` validates the full 38-identity/decision fixture and governance-vs-product separation; `semantic-negative` executes every coverage and integrity manifest case and requires exit 1 plus its exact declared code. For `evidence_expired` and `newer_failed_retest`, additionally require the declared subject ID, top-level `status=invalid`, error `freshness=stale`, and no removal of the older successful evidence record. Ensure read/usage errors return 2, invalidity/drift returns 1, and no traceback, secret-shaped value, absolute home path, or temp path enters diagnostics.</action>
  <verify>
    <automated>python3 -c 'import json,subprocess,pathlib; root=pathlib.Path("scripts/fixtures/capability-governance/invalid"); m=json.loads((root/"integrity/expected-errors.json").read_text()); rows=[(x,subprocess.run(["python3","scripts/validate-capability-governance.py",x["command"],*x["args"],"--json"],text=True,capture_output=True)) for x in m["cases"]]; docs=[(x,r,json.loads(r.stdout)) for x,r in rows]; assert all(r.returncode==1 and any(e["code"]==x["expected_code"] for e in d["errors"]) for x,r,d in docs); special=[(x,r,d) for x,r,d in docs if x["expected_code"] in {"evidence_expired","newer_failed_retest"}]; assert len(special)==2 and all(d["status"]==x["expected_status"] and any(e["code"]==x["expected_code"] and e["subject_id"]==x["subject_id"] and e["freshness"]==x["expected_freshness"] for e in d["errors"]) for x,r,d in special)' &amp;&amp; bash scripts/capability-governance-smoke.sh public-baseline &amp;&amp; bash scripts/capability-governance-smoke.sh reference-governance &amp;&amp; bash scripts/capability-governance-smoke.sh semantic-negative</automated>
  </verify>
  <acceptance_criteria>Every declared negative case returns 1 and exact code; both D-17 cases also return their exact subject, invalid status, and stale freshness while retaining history; valid bundles return 0; malformed/read/usage returns 2; outputs are sorted, redacted, and repo-relative.</acceptance_criteria>
  <done>One offline semantic gate owns cross-family, history, expiry, failed-retest, drift, path, and diagnostic invariants, and the three progressively available runner slices exercise it directly.</done>
  <commit>git add -- scripts/capability_governance.py scripts/validate-capability-governance.py scripts/capability-governance-smoke.sh &amp;&amp; git commit -m "test(01-06): close governance diagnostics"</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| JSON/paths -> bounded semantic loader | Inputs may be malformed, oversized, traversing, symlinked, or secret-bearing. |
| Family heads/ancestry -> current completion | Foreign, rewritten, or drifted evidence can spoof current proof. |
| Live repository/source/target -> frozen fingerprints | Partial comparison can hide stale decisions and results. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Elevation/Disclosure | bounded loader | high | mitigate | Root containment, no-follow resolution, byte/record caps, UTF-8 checks, redaction. |
| T-06-02 | Spoofing/Tampering | current evidence | high | mitigate | Subject/source/target/environment/proof binding plus full ancestry traversal. |
| T-06-03 | Tampering/Repudiation | drift/history | high | mitigate | Independent fingerprints, immutable prefix comparison, stable diagnostics. |
| T-06-SC | Tampering | dependency supply chain | low | accept | Python standard library and existing helper only; no install task. |
</threat_model>

<verification>
Run all three task commands, all three semantic runner slices, `python3 -m py_compile scripts/capability_governance.py scripts/validate-capability-governance.py`, and `git diff --check --` on the three files.
</verification>

<success_criteria>
- All positive fixtures pass and every negative fixture fails for its declared reason.
- Full ancestry is mandatory and every fingerprint drift exits current completion.
- Fixture/manifest evaluation time deterministically expires evidence, and the newest failed retest invalidates but does not delete older success.
- The bundle selector never becomes a completion authority or newest-file heuristic.
- CLI output is bounded, stable, redacted, offline, and package-free.
</success_criteria>

## Artifacts this phase produces

- Shared functions: `PROOF_RANK`, `counts_as_complete`, `validate_official_source_coverage`, `validate_revision_ancestry`, `detect_drift`, `validate_governance`.
- Thin CLI subcommands/flags and stable JSON diagnostics documented in `<interfaces>`.
- Exact stable errors from Plans 01-04 and 01-05, including `evidence_expired` and `newer_failed_retest` with subject/status/freshness diagnostics.
- Progressive runner slices: `public-baseline`, `reference-governance`, and `semantic-negative`.

<output>
Create `.planning/phases/01-baseline-evidence-governance/01-06-SUMMARY.md` when done.
</output>
