---
phase: 01-baseline-evidence-governance
plan: "12"
type: execute
wave: 9
depends_on:
  - 01-11
files_modified:
  - scripts/schema-contract-smoke.sh
autonomous: true
requirements:
  - COD-01
  - DIF-11
must_haves:
  truths:
    - "D-24: the existing broad schema contract gate invokes the completed focused governance runner exactly once at its final broad-gate boundary."
    - "The one-line Phase 1 integration is committed from a HEAD-derived alternate index; no pre-existing live user hunk from the dirty runner enters the commit."
    - "After commit, only the target normal-index entry is updated from a fully verified new-HEAD mode/blob; unrelated staged entries and live worktree bytes remain unchanged."
    - "Anchor mismatch, staged-path contamination, byte-preservation failure, alternate-index scope drift, tree-entry mismatch, or smoke failure blocks rather than improvising."
  artifacts:
    - path: scripts/schema-contract-smoke.sh
      provides: Existing repository-wide schema gate with one focused governance invocation
  key_links:
    - from: scripts/schema-contract-smoke.sh
      to: scripts/capability-governance-smoke.sh
      via: exactly one final-boundary invocation after the commercial handoff smoke
      pattern: ^bash scripts/capability-governance-smoke\.sh$
---

<objective>
将 focused capability-governance runner 精确接入已有 broad schema gate，同时用 alternate Git index 证明 Phase 1 commit 只包含一个新增调用且保留 live dirty user hunks。

Purpose: 完成 D-24、AGENTS 和 PATTERNS 要求的 broad integration，不把用户正在修改的 `scripts/schema-contract-smoke.sh` 整体暂存或吸收到 Phase 1 commit。
Output: `scripts/schema-contract-smoke.sh` 在最终成功消息前恰好调用一次 `bash scripts/capability-governance-smoke.sh`，以及一个只含该行的原子 commit。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-baseline-evidence-governance/01-11-SUMMARY.md
@.planning/phases/01-baseline-evidence-governance/01-PATTERNS.md
@scripts/schema-contract-smoke.sh

<interfaces>
The only Phase 1 source change is the exact line `bash scripts/capability-governance-smoke.sh`.
The exact insertion anchor must occur once and be byte-identical in both `HEAD:scripts/schema-contract-smoke.sh` and the live dirty file: the line `bash scripts/commercial-release-handoff-smoke.sh &gt;/dev/null`, one blank line, then `echo "schema contract smoke passed"`. Insert the governance invocation immediately after the handoff-smoke line and before that blank line/final success message.
Normal-index add/staging flows for this dirty path, whole dirty-file staging, normal-index reset/checkout operations, and destructive worktree operations are forbidden. The task uses a temporary alternate index initialized from the preflight HEAD; only after validating the new commit relationship and exact tree entry may it update the target normal-index entry with path-specific `git update-index --cacheinfo "$mode,$blob,$path"`, without changing the worktree or any unrelated index entry.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the focused runner with alternate-index preservation proof</name>
  <files>scripts/schema-contract-smoke.sh</files>
  <read_first>scripts/schema-contract-smoke.sh, scripts/capability-governance-smoke.sh, .planning/phases/01-baseline-evidence-governance/01-PATTERNS.md, .planning/phases/01-baseline-evidence-governance/01-11-SUMMARY.md</read_first>
  <behavior>
    - Preflight requires no normal-index staged change for this path, zero existing exact governance invocations, and one byte-identical insertion anchor in both HEAD and the live dirty file.
    - Removing only the newly inserted invocation from the post-edit live file reproduces the complete preflight live bytes; removing it from the HEAD-derived phase-only copy reproduces the complete preflight HEAD bytes.
    - The alternate-index commit changes exactly this path by exactly one added invocation, after which the normal index matches new HEAD for the path while all original user changes remain unstaged in the live worktree.
    - Both the focused gate and the broad `bash scripts/schema-contract-smoke.sh` gate pass with the invocation present exactly once.
  </behavior>
  <action>Use a temporary directory, set `path=scripts/schema-contract-smoke.sh`, and record `start_head`. First require `git diff --cached --quiet -- "$path"`; if the path has any staged bytes, block. Snapshot every unrelated normal-index entry with `git ls-files --stage -z -- . ":(exclude)$path"` so the exact mode/blob/stage tuples can be compared after the target-only update. Read the single `git ls-tree "$start_head" -- "$path"` entry; require exactly one entry, the original path, object type `blob`, and record `start_mode` plus `start_blob`, with `start_blob` equal to `git rev-parse "$start_head:$path"`. Snapshot the complete live bytes, the complete `HEAD:` bytes, `git diff --binary --no-ext-diff -- "$path"`, and SHA-256 hashes of those artifacts into the temporary directory. Require the exact invocation count to be zero in both versions. Locate the exact anchor from `<interfaces>` in each version, require exactly one occurrence, and require the anchor bytes to be identical. If any precondition differs, stop before editing or committing.

Create a phase-only temporary copy from `HEAD:scripts/schema-contract-smoke.sh`. Use `apply_patch` on that temporary copy to insert the one exact invocation at the exact anchor. In a separate `apply_patch` call, insert the same one exact line at the same anchor in the live dirty file. Do not use a formatter, generated whole-file replacement, `sed -i`, or normal Git staging. Require the exact invocation count to be one in both edited copies and require every other anchor byte to remain unchanged.

Prove preservation before commit: copy the edited live file to a second temporary comparison file, use `apply_patch` to remove only the exact inserted invocation from that comparison file, and `cmp` it byte-for-byte with the preflight live snapshot. Perform the same remove-one-line/`cmp` proof between the phase-only copy and preflight HEAD snapshot. Snapshot the edited live bytes/hash, run `bash scripts/capability-governance-smoke.sh`, then run `bash scripts/schema-contract-smoke.sh`. Any anchor, count, byte-preservation, or smoke failure blocks before commit; do not choose a nearby anchor or rewrite surrounding user content.

Build an alternate index only after all precommit proofs pass: set `GIT_INDEX_FILE` to a new temporary index path that does not yet exist, run `GIT_INDEX_FILE="$phase_index" git read-tree "$start_head"`, write the phase-only file as `phase_blob`, and update only this path with `GIT_INDEX_FILE="$phase_index" git update-index --add --cacheinfo "$start_mode,$phase_blob,$path"`. Require the alternate entry to be one blob at the exact path with `start_mode` and `phase_blob`. Before committing, require current `git rev-parse HEAD` to still equal `start_head`; require the alternate cached name set to equal only this path; require its cached patch to contain no deletion and exactly one added line equal to the invocation; run the alternate-index cached diff check. Commit with `GIT_INDEX_FILE="$phase_index" git commit -m "test(01-12): wire governance broad contract gate"`. Never use a normal-index add/staging flow for this path.

After the alternate-index commit advances HEAD, record `new_head` and perform every commit/tree proof before touching the normal index: require `new_head` to differ from `start_head`, current `HEAD` to remain `new_head`, `git rev-parse "$new_head^"` to equal `start_head`, the commit name set to equal only `path`, and its patch to contain exactly one invocation addition with no deletion. Read the exact `git ls-tree "$new_head" -- "$path"` result and require one entry, object type `blob`, the exact path, `head_mode` equal to `start_mode` (including the original executable-bit state), and `head_blob` equal both to `git rev-parse "$new_head:$path"` and the already verified `phase_blob`. If any relationship, count, mode, type, blob, or path invariant fails, stop without changing the normal index.

Immediately re-require current `HEAD` to equal `new_head`, then update only the target normal-index entry with `git update-index --cacheinfo "$head_mode,$head_blob,$head_path"`; do not mutate the worktree. Snapshot `git ls-files --stage -z -- . ":(exclude)$path"` again and `cmp` it byte-for-byte with the preflight unrelated-index snapshot, proving every unrelated staged/index entry is unchanged. Require the normal staged diff for the path to be empty, the normal index entry for the path to equal the verified new-HEAD mode/blob/path at stage zero, the live file to remain unstaged/dirty, the live edited-byte hash to match the precommit edited snapshot, and the remove-only-invocation comparison to still equal the preflight live bytes. Finally rerun `bash scripts/capability-governance-smoke.sh` and `bash scripts/schema-contract-smoke.sh`. If any postcommit proof fails, report the exact invariant and do not use a normal-index reset/checkout operation, a normal staging flow, or a whole-file replacement to conceal it.</action>
  <verify>
    <automated>path=scripts/schema-contract-smoke.sh &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD)" = "$path" &amp;&amp; git show --format= --unified=0 HEAD -- "$path" | python3 -c 'import sys; lines=sys.stdin.read().splitlines(); added=[x for x in lines if x.startswith("+") and not x.startswith("+++")]; removed=[x for x in lines if x.startswith("-") and not x.startswith("---")]; assert added==["+bash scripts/capability-governance-smoke.sh"],added; assert not removed,removed' &amp;&amp; git diff --cached --quiet -- "$path" &amp;&amp; if git diff --quiet -- "$path"; then exit 1; fi &amp;&amp; test "$(awk '$0=="bash scripts/capability-governance-smoke.sh"{n++} END{print n+0}' "$path")" -eq 1 &amp;&amp; bash scripts/capability-governance-smoke.sh &amp;&amp; bash scripts/schema-contract-smoke.sh</automated>
  </verify>
  <acceptance_criteria>The commit contains exactly one added governance-runner invocation and no user hunk; the target normal-index entry equals the verified new-HEAD mode/blob/path while every unrelated staged/index entry is unchanged; normal staged diff is empty for the target; the complete preflight user bytes are reproducible by removing only that line; live user changes remain unstaged; focused and broad gates pass.</acceptance_criteria>
  <done>D-24 broad integration is present exactly once and independently committed without treating a dirty-file staging shortcut as preservation proof.</done>
  <commit>Do not use the executor's normal add/staging commit path. Create the commit inside this task only after precommit proofs pass, using the HEAD-derived `GIT_INDEX_FILE` alternate index and the exact commit message above; then validate the new-HEAD parent and tree entry before updating only the target normal-index cache entry from its verified mode/blob/path.</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Dirty live runner -> Phase 1 patch | User-owned hunks can be accidentally staged or rewritten with the one-line integration. |
| HEAD-derived alternate index -> branch commit | A malformed index or wrong path/mode/blob can commit more than the intended invocation. |
| Focused runner -> broad contract success | Missing, duplicate, or misplaced invocation can leave governance schemas outside the repository gate. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-01 | Tampering/Repudiation | dirty worktree preservation | high | mitigate | Dual apply-patch edits, remove-one-line byte comparisons, pre/post hashes, and block-before-commit anchors. |
| T-12-02 | Tampering/Elevation | alternate and normal Git indexes | high | mitigate | Alternate index starts from exact HEAD and commits one verified cacheinfo path; the new-HEAD parent/tree entry is revalidated before a target-only normal-index cacheinfo update, with unrelated index entries compared byte-for-byte. |
| T-12-03 | Spoofing | broad gate wiring | high | mitigate | Exact invocation count/location plus focused and full broad smoke before and after commit. |
| T-12-SC | Tampering | dependency supply chain | low | accept | No package or dependency change; this task wires an already validated local runner. |
</threat_model>

<verification>
Run the task's preflight, byte-preservation, alternate-index, new-HEAD parent/tree-entry, target-only normal-index cacheinfo, unrelated-index preservation, focused-smoke, and broad-smoke proofs. Inspect no worktree path outside `scripts/schema-contract-smoke.sh`; never use broad staging or a destructive worktree operation.
</verification>

<success_criteria>
- `scripts/schema-contract-smoke.sh` invokes `bash scripts/capability-governance-smoke.sh` exactly once after the commercial handoff smoke and before its final success message.
- The Phase 1 commit diff contains exactly that one added line and preserves the original tracked mode, including its executable-bit state.
- The normal index matches the verified new-HEAD mode/blob/path only for the target, every unrelated staged/index entry is unchanged, and no worktree byte is changed by that update.
- Removing only that line from live bytes reproduces the preflight dirty file byte-for-byte; normal index is clean for the path and user changes remain unstaged.
- Focused governance smoke and the full existing schema contract smoke pass.
</success_criteria>

## Artifacts this phase produces

- One exact broad-gate invocation in `scripts/schema-contract-smoke.sh`.
- One phase-only commit whose diff contains no user-owned runner hunk.
- Ephemeral pre/post byte, diff, and alternate-index proofs retained only for task verification, not as product evidence.

<output>
Create `.planning/phases/01-baseline-evidence-governance/01-12-SUMMARY.md` when done.
</output>
