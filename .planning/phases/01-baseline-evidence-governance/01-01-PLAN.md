---
phase: 01-baseline-evidence-governance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/schemas/kiana-official-source-artifact.v1.schema.json
  - docs/schemas/kiana-public-parity-baseline.v1.schema.json
  - docs/schemas/kiana-reference-repository-registry.v1.schema.json
  - docs/schemas/kiana-capability-decisions.v1.schema.json
autonomous: true
requirements:
  - COD-01
  - DIF-11
must_haves:
  truths:
    - "D-01..D-05: every normalized official source entry is represented inside the public-baseline family and maps to governed capability IDs or a reviewed exclusion."
    - "D-07..D-12: all repository identities, compatibility labels, aliases, and capability decisions use one closed cross-plan shape without implying product completion."
    - "D-19..D-21/D-24: the four files are closed versioned contracts that reuse the checked-in structural validator and reserve stable semantic errors."
  artifacts:
    - path: docs/schemas/kiana-official-source-artifact.v1.schema.json
      provides: Controlled normalized official-source artifact contract
    - path: docs/schemas/kiana-public-parity-baseline.v1.schema.json
      provides: Public journey, capability, and official-source-index contract
    - path: docs/schemas/kiana-reference-repository-registry.v1.schema.json
      provides: Frozen repository identity, revision, license, domains, and aliases contract
    - path: docs/schemas/kiana-capability-decisions.v1.schema.json
      provides: Capability-level Adopt/Adapt/Reject review contract
  key_links:
    - from: docs/schemas/kiana-public-parity-baseline.v1.schema.json
      to: docs/schemas/kiana-official-source-artifact.v1.schema.json
      via: source_artifact path/hash and source_entry_id bindings
      pattern: source_entry_id
    - from: docs/schemas/kiana-capability-decisions.v1.schema.json
      to: docs/schemas/kiana-reference-repository-registry.v1.schema.json
      via: repo_id and inventory capability references
      pattern: repo_id
---

<objective>
冻结 COD-01/DIF-11 的 source/public/reference/decision contracts，并一次性消除 official source completeness、ancestry 字段和 alias 命名歧义。

Purpose: 为 D-01..D-12、D-19..D-21、D-24 提供后续 validator、fixtures 和 canonical revisions 共同消费的唯一字段合同。
Output: 四个互相引用但生命周期独立的 closed JSON schemas。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-baseline-evidence-governance/01-CONTEXT.md
@.planning/phases/01-baseline-evidence-governance/01-RESEARCH.md
@.planning/phases/01-baseline-evidence-governance/01-PATTERNS.md

<interfaces>
Every canonical revision root uses exact top-level ancestry fields: `revision_id`, `revision_kind` (`genesis|successor`), and either `genesis_reason` or all of `previous_revision_id`, `previous_revision_path`, `previous_revision_sha256`.
Official-source coverage errors are exactly `missing_required_capability`, `unmapped_source_entry`, `duplicate_source_mapping`, and `unjustified_exclusion`.
Repository compatibility uses `domains` only as imported classification labels. `aliases` is an array of objects with exact keys `kind`, `value`, `change_kind`, `reason`, `effective_at`, `evidence_ids`, `review_revision`; no alternate alias field is permitted.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze controlled official-source and public-baseline contracts</name>
  <files>docs/schemas/kiana-official-source-artifact.v1.schema.json, docs/schemas/kiana-public-parity-baseline.v1.schema.json</files>
  <read_first>docs/schemas/kiana-commercial-release-blockers.v1.schema.json, scripts/validate-json-schema.py, .planning/phases/01-baseline-evidence-governance/01-CONTEXT.md, .planning/phases/01-baseline-evidence-governance/01-RESEARCH.md</read_first>
  <behavior>
    - A normalized source artifact requires artifact identity, official URI, immutable archive URI, retrieval metadata, normalized-content hash, and stable entries.
    - A public revision requires all 15 journey IDs, required/optional capability membership, independently evidenced capability rows, and a source-artifact path/hash binding.
    - Every artifact entry is covered by exactly one `official_source_index` record containing non-empty `capability_ids` or an `exclusion` with classification, rationale, evidence IDs, and review revision.
  </behavior>
  <action>Create both draft-2020-12 schemas with stable `$id`/instance `schema`, `$defs`, required fields, conditional ancestry, and `additionalProperties: false` at every closed object. The source artifact records `artifact_id`, `source_kind=official_public`, `official_uri`, `archive_uri`, `retrieved_at`, `applicable_product_version`, `normalization`, `content_sha256`, and `entries[]` with stable `source_entry_id`, locator, title, kind, and entry hash. The public contract keeps the source mapping inside the public-baseline family: `source_artifact`, `official_source_index`, `journeys`, and `capabilities` are sibling fields. A mapping is exclusive between non-empty capability IDs and a reviewed exclusion classified as `not_public_contract|duplicate_navigation|non_capability_metadata|out_of_scope_product`. Journeys expose explicit required and optional capability ID arrays; capability rows carry D-02/D-06 state, proof, freshness, owner, target phase, evidence, gap, and difference fields. Schema shape does not assert semantic completeness; reserve the four stable errors in descriptions for Plan 01-06.</action>
  <verify>
    <automated>python3 -m json.tool docs/schemas/kiana-official-source-artifact.v1.schema.json &gt;/dev/null &amp;&amp; python3 -m json.tool docs/schemas/kiana-public-parity-baseline.v1.schema.json &gt;/dev/null &amp;&amp; python3 -c 'import json; from pathlib import Path; files=[Path("docs/schemas/kiana-official-source-artifact.v1.schema.json"),Path("docs/schemas/kiana-public-parity-baseline.v1.schema.json")]; docs=[json.loads(p.read_text()) for p in files]; assert all(d.get("additionalProperties") is False for d in docs); text=files[1].read_text(); assert all(x in text for x in ("official_source_index","source_entry_id","previous_revision_sha256","genesis_reason"))'</automated>
  </verify>
  <acceptance_criteria>Both schemas parse, close root and nested records, encode mutually exclusive genesis/successor ancestry, and make official source entries structurally addressable without creating a fifth completion authority.</acceptance_criteria>
  <done>The public family can prove source-entry coverage and strict journey membership from stable IDs while retaining immutable source artifact identity.</done>
  <commit>git add -- docs/schemas/kiana-official-source-artifact.v1.schema.json docs/schemas/kiana-public-parity-baseline.v1.schema.json &amp;&amp; git commit -m "test(01-01): freeze public source contracts"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze repository identity and capability-decision contracts</name>
  <files>docs/schemas/kiana-reference-repository-registry.v1.schema.json, docs/schemas/kiana-capability-decisions.v1.schema.json</files>
  <read_first>docs/schemas/kiana-commercial-release-blockers.v1.schema.json, docs/agent-program/kiana-completion/references.json, .planning/phases/01-baseline-evidence-governance/01-CONTEXT.md, .planning/phases/01-baseline-evidence-governance/01-PATTERNS.md</read_first>
  <behavior>
    - Git-backed identities require real `git_head`, normalized `tree_sha256`, and `license_sha256`; content-only identities require `revision_kind=content_tree_sha256` and normalized tree hash.
    - `domains` is a string array of imported compatibility classification labels, not proof or completion state.
    - Every alias object uses the exact shared shape and every decision binds source revision, Kiana target Git/artifact revision, license review, owner, risk, tests, evidence, and review revision.
  </behavior>
  <action>Per D-07 and D-08, create closed registry and decision schemas with the shared top-level ancestry fields and capability-level rather than repository-level decisions. Registry records require stable `repo_id`, normalized current path, upstream identity, availability, scan time, freshness, revision kind/value, tree hash, license hash/status/expression/compatibility/evidence, imported `domains`, exact-shape `aliases`, and `inventory_capability_ids`. Alias `kind` is `repo_id|path`; `change_kind` is `renamed|removed|replaced`; all remaining keys are required. Decisions require `decision_id`, `repo_id`, `capability_id`, source path/revision, `adopt|adapt|reject`, reason, license compatibility/evidence, security/risk, target owner/path plus `target_revision_kind` and `target_revision_value`, tests, evidence IDs, review revision, and freshness. Encode conditional required fields for Reject and current Adopt/Adapt without adding a repository-level verdict or product-completion flag.</action>
  <verify>
    <automated>python3 -m json.tool docs/schemas/kiana-reference-repository-registry.v1.schema.json &gt;/dev/null &amp;&amp; python3 -m json.tool docs/schemas/kiana-capability-decisions.v1.schema.json &gt;/dev/null &amp;&amp; python3 -c 'from pathlib import Path; r=Path("docs/schemas/kiana-reference-repository-registry.v1.schema.json").read_text(); d=Path("docs/schemas/kiana-capability-decisions.v1.schema.json").read_text(); assert all(x in r for x in ("domains","aliases","change_kind","license_sha256","tree_sha256","git_head")); assert all(x in d for x in ("target_revision_kind","target_revision_value","review_revision"))'</automated>
  </verify>
  <acceptance_criteria>Both schemas parse; the exact aliases/domains contract is unambiguous; Git, content-only, license, and Kiana target revisions are structurally distinguishable; no repository-wide verdict can substitute for per-capability review.</acceptance_criteria>
  <done>All 38 registry records and all Adopt/Adapt/Reject records can share one exact identity and revision contract across fixtures, validator, data, and renderer.</done>
  <commit>git add -- docs/schemas/kiana-reference-repository-registry.v1.schema.json docs/schemas/kiana-capability-decisions.v1.schema.json &amp;&amp; git commit -m "test(01-01): freeze reference review contracts"</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Official artifact -> public baseline | Remote-derived entries and hashes are untrusted until structurally and semantically bound. |
| `reference/` metadata -> registry/decisions | Paths, Git metadata, license text, aliases, and target revisions can be spoofed or stale. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-01 | Spoofing/Tampering | source-entry mapping | high | mitigate | Closed artifact binding, exclusive map/exclusion records, stable IDs, and semantic oracle errors. |
| T-01-02 | Spoofing | repository and target revisions | high | mitigate | Separate HEAD/tree/license/target fingerprints and conditional revision fields. |
| T-01-03 | Tampering | alias history | medium | mitigate | One closed alias object shape with evidence and review revision. |
| T-01-SC | Tampering | dependency supply chain | low | accept | This plan installs no package and changes only JSON contracts. |
</threat_model>

<verification>
Run both task commands and `git diff --check --` over the four schema files; structural success is not treated as semantic ledger success.
</verification>

<success_criteria>
- Official source coverage is expressible inside the public family with exact oracle names.
- Registry aliases/domains and decision target bindings have one cross-plan spelling and shape.
- Every canonical family schema in this plan supports mandatory genesis/successor ancestry.
- No package, network client, runtime adapter, or dirty legacy file is touched.
</success_criteria>

## Artifacts this phase produces

- Public source contracts: `kiana-official-source-artifact` and `kiana-public-parity-baseline` schemas.
- Reference contracts: `kiana-reference-repository-registry` and `kiana-capability-decisions` schemas.
- Shared ancestry fields: `revision_id`, `revision_kind`, `genesis_reason`, `previous_revision_id`, `previous_revision_path`, `previous_revision_sha256`.
- Shared alias fields: `kind`, `value`, `change_kind`, `reason`, `effective_at`, `evidence_ids`, `review_revision`.

<output>
Create `.planning/phases/01-baseline-evidence-governance/01-01-SUMMARY.md` when done.
</output>
