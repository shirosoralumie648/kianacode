---
phase: 01-baseline-evidence-governance
plan: "03"
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - scripts/capability-governance-smoke.sh
  - scripts/fixtures/capability-governance/valid/minimal-graph.json
  - scripts/fixtures/capability-governance/valid/full-38-repositories.json
  - scripts/fixtures/capability-governance/valid/hostile-rendering.json
  - scripts/fixtures/capability-governance/valid/offline-source-identity.json
autonomous: true
requirements:
  - COD-01
  - DIF-11
must_haves:
  truths:
    - "D-01..D-21: valid fixtures exercise all four canonical families, mandatory ancestry, exact aliases, strict proof axes, and 38 unique identities before production data is authored."
    - "D-22/D-24: an offline Bash/Python runner provides bounded structural and fixture-shape sampling without a package or Cargo build."
  artifacts:
    - path: scripts/capability-governance-smoke.sh
      provides: Focused offline fixture and contract runner
    - path: scripts/fixtures/capability-governance/valid/minimal-graph.json
      provides: Small complete cross-family graph with predecessor traversal
    - path: scripts/fixtures/capability-governance/valid/full-38-repositories.json
      provides: Exact 38-identity registry/decision fixture
    - path: scripts/fixtures/capability-governance/valid/offline-source-identity.json
      provides: Controlled normalized official-source artifact identity fixture
  key_links:
    - from: scripts/capability-governance-smoke.sh
      to: scripts/validate-json-schema.py
      via: existing structural validator for every versioned fixture object
      pattern: validate-json-schema.py
---

<objective>
建立独立的 valid fixture/runner slice，使 schema、38 identity、hostile rendering 和 offline source identity 在 semantic code 之前即可重复采样。

Purpose: 满足 D-01..D-24 的 Nyquist Wave-0 正例基础，并把 no-new-package、offline、低延迟约束变成命令。
Output: 一个 focused runner 和四个相互补充的 valid fixture bundles。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-baseline-evidence-governance/01-01-SUMMARY.md
@.planning/phases/01-baseline-evidence-governance/01-02-SUMMARY.md
@.planning/phases/01-baseline-evidence-governance/01-VALIDATION.md
@scripts/schema-contract-smoke.sh

<interfaces>
This plan's runner exposes only `schemas` and `fixture-shapes`; Plans 01-06, 01-07, 01-10, and 01-11 extend the same file sequentially with semantic, drift-refresh, legacy-authority, generated-view, and production slices after their CLIs/data exist.
Fixture bundles use exact top-level keys `official_source_artifact`, `public_baseline_revisions`, `repository_registry_revisions`, `capability_decision_revisions`, `evidence_revisions`, `legacy_authority_revisions`, and `bundle_manifest`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the offline runner and complete small graph</name>
  <files>scripts/capability-governance-smoke.sh, scripts/fixtures/capability-governance/valid/minimal-graph.json</files>
  <read_first>scripts/schema-contract-smoke.sh, scripts/validate-json-schema.py, docs/schemas/kiana-public-parity-baseline.v1.schema.json, docs/schemas/kiana-capability-evidence-index.v1.schema.json</read_first>
  <behavior>
    - `schemas` parses all eight Phase 1 schemas and structurally validates each family object extracted from the small graph.
    - `fixture-shapes` verifies exact bundle keys, mandatory genesis/successor ancestry, unique IDs, and path/hash-only head bindings.
    - Unknown or missing slice names return usage status 2; successful slices print measured elapsed seconds and remain offline.
  </behavior>
  <action>Create a Bash 5 fail-fast runner using the repository-root/Python fallback pattern of the existing schema smoke. The `schemas` slice must parse and enumerate exactly these eight contracts: official-source artifact, public baseline, repository registry, capability decisions, evidence index, governance diff, legacy authority classification, and governance bundle; then use the existing structural validator on every applicable object extracted from the fixture graph. Use Python `json` to extract fixture objects into a temporary directory; do not parse JSON in shell. Create a small graph containing genesis and successor revisions for public baseline, registry, decisions, evidence, and legacy classification, with exact source-entry mappings, one Git repository, one content-only repository, exact alias objects, and matching bundle hashes. Implement only the two slice names owned here and record elapsed time; fail when a slice reaches 30 seconds or any command attempts network access.</action>
  <verify>
    <automated>bash scripts/capability-governance-smoke.sh schemas &amp;&amp; bash scripts/capability-governance-smoke.sh fixture-shapes</automated>
  </verify>
  <acceptance_criteria>Both slices exit 0 below 30 seconds without network/Cargo; unknown input exits 2; the graph includes real predecessor traversal rather than an optional previous index.</acceptance_criteria>
  <done>Downstream semantic code has a deterministic positive cross-family graph and an established focused runner.</done>
  <commit>git add -- scripts/capability-governance-smoke.sh scripts/fixtures/capability-governance/valid/minimal-graph.json &amp;&amp; git commit -m "test(01-03): add governance fixture runner"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode all 38 identities and the exact compatibility shape</name>
  <files>scripts/fixtures/capability-governance/valid/full-38-repositories.json</files>
  <read_first>docs/agent-program/kiana-completion/references.json, docs/schemas/kiana-reference-repository-registry.v1.schema.json, docs/schemas/kiana-capability-decisions.v1.schema.json, .planning/phases/01-baseline-evidence-governance/01-CONTEXT.md</read_first>
  <behavior>
    - The registry has exactly 38 unique stable IDs and normalized current paths matching the live top-level `reference/` directory.
    - Raw legacy IDs/paths survive only through exact-shape `aliases`; `domains` values are imported compatibility labels.
    - Every inventory capability has exactly one current capability-level decision and governance totals remain separate from proof state.
  </behavior>
  <action>Build the full registry/decision fixture from the checked-in 38-entry seed and live directory names. Git-backed entries contain representative HEAD/tree/license fingerprints; `reference/claude-code-main (2)` and `reference/claude-code-rev-main` exercise `content_tree_sha256`. Preserve every legacy ID/path through alias objects with explicit change kind, reason, effective time, evidence IDs, and review revision. Keep `domains` as imported labels and add at least one Adopt, Adapt, and Reject decision with all conditional fields. Do not infer product implementation from decision state.</action>
  <verify>
    <automated>python3 -m json.tool scripts/fixtures/capability-governance/valid/full-38-repositories.json &gt;/dev/null &amp;&amp; python3 -c 'import json; from pathlib import Path; d=json.loads(Path("scripts/fixtures/capability-governance/valid/full-38-repositories.json").read_text()); rows=d["repository_registry_revisions"][-1]["repositories"]; assert len(rows)==38==len({x["repo_id"] for x in rows})==len({x["path"] for x in rows}); assert all("domains" in x and all(set(a)=={"kind","value","change_kind","reason","effective_at","evidence_ids","review_revision"} for a in x["aliases"]) for x in rows)'</automated>
  </verify>
  <acceptance_criteria>The fixture reconciles 38 unique live paths, uses no alternate alias field, covers Git and content-only revisions, and contains one decision per inventory capability.</acceptance_criteria>
  <done>DIF-11 identity/alias/decision semantics have a full-size valid oracle independent of production data.</done>
  <commit>git add -- scripts/fixtures/capability-governance/valid/full-38-repositories.json &amp;&amp; git commit -m "test(01-03): add 38-reference valid fixture"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add hostile rendering and offline official-source identity fixtures</name>
  <files>scripts/fixtures/capability-governance/valid/hostile-rendering.json, scripts/fixtures/capability-governance/valid/offline-source-identity.json</files>
  <read_first>docs/schemas/kiana-official-source-artifact.v1.schema.json, docs/schemas/kiana-public-parity-baseline.v1.schema.json, .planning/phases/01-baseline-evidence-governance/01-RESEARCH.md, .planning/phases/01-baseline-evidence-governance/01-VALIDATION.md</read_first>
  <behavior>
    - Hostile-but-valid text includes Markdown delimiters, brackets, HTML-like text, newlines, and safe HTTPS archive URIs for later escaping checks.
    - Offline source identity binds normalized bytes, immutable archive URI, retrieval metadata, entry hashes, and public mapping without requiring network access.
    - Every source entry maps exactly once to capability IDs or a fully reviewed exclusion.
  </behavior>
  <action>Create two valid bundles. The hostile bundle uses untrusted-looking text only in fields that the renderer must encode; it contains no executable markup, secret, or absolute path. The offline-source bundle embeds a controlled normalized artifact, expected SHA-256, archive identity, and a matching public source index with both mapped and justified-exclusion entries. Include a deterministic byte representation that Plan 01-07 can freeze and rehash offline. Ensure the bundle itself is not a separate completion authority.</action>
  <verify>
    <automated>python3 -m json.tool scripts/fixtures/capability-governance/valid/hostile-rendering.json &gt;/dev/null &amp;&amp; python3 -m json.tool scripts/fixtures/capability-governance/valid/offline-source-identity.json &gt;/dev/null &amp;&amp; bash scripts/capability-governance-smoke.sh fixture-shapes</automated>
  </verify>
  <acceptance_criteria>Both fixtures parse; offline identity can be checked without retrieval; every artifact entry has one mapping/exclusion; hostile strings contain no real credential or local absolute path.</acceptance_criteria>
  <done>Source retention and rendering safety have positive fixtures before their production CLIs land.</done>
  <commit>git add -- scripts/fixtures/capability-governance/valid/hostile-rendering.json scripts/fixtures/capability-governance/valid/offline-source-identity.json &amp;&amp; git commit -m "test(01-03): add source and rendering fixtures"</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Fixture JSON -> smoke runner | Malformed, oversized, or mislabeled fixture data can create false green tests. |
| Untrusted strings -> later renderer | Valid metadata can still inject Markdown/HTML or disclose paths. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Tampering | fixture extraction | high | mitigate | Python JSON parsing, schema validation, exact bundle keys, and fail-fast commands. |
| T-03-02 | Spoofing | 38-identity fixture | high | mitigate | Unique ID/path plus exact live seed reconciliation. |
| T-03-03 | Tampering/Information Disclosure | hostile strings | high | mitigate | Synthetic values, no real secrets/absolute paths, later exact escaping tests. |
| T-03-SC | Tampering | dependency supply chain | low | accept | Runner uses existing Bash/Python only and performs no install. |
</threat_model>

<verification>
Run both runner slices, then `git diff --check --` over the five plan files. Record elapsed output for Nyquist sampling.
</verification>

<success_criteria>
- Valid fixtures cover small cross-family history, full 38 identity, hostile rendering, and offline source identity.
- Every task has a sub-30-second offline command.
- Exact aliases/domains and mandatory ancestry appear in positive data.
- No semantic implementation, package, dirty legacy edit, or broad smoke integration occurs here.
</success_criteria>

## Artifacts this phase produces

- Focused runner slices: `schemas` over all eight contracts and `fixture-shapes`.
- Positive fixture bundles: small graph, 38 references, hostile rendering, offline source identity.
- Shared fixture bundle keys documented in `<interfaces>`.

<output>
Create `.planning/phases/01-baseline-evidence-governance/01-03-SUMMARY.md` when done.
</output>
