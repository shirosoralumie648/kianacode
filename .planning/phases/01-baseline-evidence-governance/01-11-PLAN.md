---
phase: 01-baseline-evidence-governance
plan: "11"
type: execute
wave: 8
depends_on:
  - 01-03
  - 01-06
  - 01-10
files_modified:
  - scripts/capability_governance.py
  - scripts/generate-capability-governance.py
  - scripts/capability-governance-smoke.sh
  - docs/agent-program/kiana-completion/governance/generated/public-parity.md
  - docs/agent-program/kiana-completion/governance/generated/reference-governance.md
  - docs/agent-program/kiana-completion/governance/generated/legacy-authority.md
  - docs/agent-program/kiana-completion/governance/diffs/public-baseline/cc-public-2026-07-15.genesis-to-current.json
  - docs/agent-program/kiana-completion/governance/diffs/repository-registry/references-2026-07-15.genesis-to-current.json
  - docs/reference_audit/CAPABILITY-GOVERNANCE.generated.txt
autonomous: true
requirements:
  - COD-01
  - DIF-11
must_haves:
  truths:
    - "D-05/D-06/D-12/D-18/D-19/D-22: deterministic views/diffs derive only from validated explicit heads and keep public, governance, product, proof, and freshness projections separate."
    - "D-20/D-23: legacy status inputs remain untouched and a generated directory warning/index points readers to canonical replacement views."
    - "D-21/D-24: real default-render, compatibility, diff, tamper, and final production commands prove exact bytes, safe roots, ancestry, source coverage, 38 identities, drift, and legacy hashes offline."
  artifacts:
    - path: scripts/generate-capability-governance.py
      provides: Deterministic render/render-compat/diff CLI
    - path: docs/agent-program/kiana-completion/governance/generated/public-parity.md
      provides: Human COD-01 ledger with snapshot/source/proof blockers
    - path: docs/agent-program/kiana-completion/governance/generated/reference-governance.md
      provides: Human 38-reference governance ledger
    - path: docs/agent-program/kiana-completion/governance/generated/legacy-authority.md
      provides: Per-path legacy authority replacement index
    - path: scripts/capability-governance-smoke.sh
      provides: Final generated-views, production, and no-argument aggregate gates
  key_links:
    - from: scripts/generate-capability-governance.py
      to: scripts/capability_governance.py
      via: mandatory validate_governance and counts_as_complete before render/diff
      pattern: validate_governance
    - from: scripts/capability-governance-smoke.sh
      to: docs/agent-program/kiana-completion/governance/current.json
      via: explicit production head validation and full ancestry traversal
      pattern: production
---

<objective>
生成 deterministic public/reference/legacy views 与 ancestry-bound diffs，并用真实 CLI confinement/tamper checks 和最终 focused production gate 完成 Phase 1 handoff。

Purpose: 落实 D-05/D-06、D-12..D-24，关闭 default-render output escape、diff CLI behavior、legacy authority、ancestry 和 Nyquist integration blockers。
Output: shared renderer functions、薄 generator CLI、三份 views、两份 diffs、目录 warning/index，以及 final offline runner。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-baseline-evidence-governance/01-10-SUMMARY.md
@scripts/capability_governance.py
@scripts/capability-governance-smoke.sh
@docs/agent-program/kiana-completion/governance/current.json
@docs/agent-program/kiana-completion/governance/compat-outputs.json

<interfaces>
Exact CLI: `render --manifest PATH --output-root PATH [--check]`; `render-compat --manifest PATH --output-root PATH --repository-output-root PATH --compat-manifest PATH [--check]`; `diff --family public-baseline|repository-registry --from PATH --to PATH --output PATH [--check]`.
`render` may write only beneath `--output-root`; a manifest that attempts to carry a repository compatibility output is rejected before writes. Repository paths are available only to `render-compat`, only beneath explicit `--repository-output-root`, and only when allowlisted by `--compat-manifest`.
Diff documents use exact bindings `from_revision_id`, `from_revision_sha256`, `to_revision_id`, and `to_revision_sha256`, calculated from the CLI input files.
Final runner slices are `schemas`, `fixture-shapes`, `public-baseline`, `reference-governance`, `semantic-negative`, `drift-refresh`, `legacy-authority`, `generated-views`, and `production`; no argument runs all in dependency order.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement and directly gate deterministic render confinement</name>
  <files>scripts/capability_governance.py, scripts/generate-capability-governance.py</files>
  <read_first>scripts/capability_governance.py, scripts/fixtures/capability-governance/valid/hostile-rendering.json, docs/schemas/kiana-capability-governance-diff.v1.schema.json, scripts/commercial-release-blockers-report.sh</read_first>
  <behavior>
    - Repeated default render bytes are identical, `--check` never rewrites, invalid canonical input causes zero writes, and hostile values are encoded.
    - Default `render` rejects a derived manifest that attempts a repository compatibility path and leaves every path outside its explicit output root untouched.
    - `render-compat` requires two explicit temporary roots plus an allowlist manifest, writes only allowlisted repository paths, repeats byte-for-byte, and does not change bytes or mtimes under `--check`.
  </behavior>
  <action>Add escaping, deterministic sorting, summary recomputation, public/reference/legacy rendering, revision diffing, exact-byte write/check, and atomic output helpers to the shared module without duplicating validation/completion. Use canonical frozen/review times, UTF-8/LF, one final newline, generated/do-not-edit marker, explicit IDs, and visible blockers; encode table/link/control/HTML-like input and omit secrets/absolute/temp paths. Implement the thin CLI with the exact commands/flags above. Default `render` preflights the complete manifest and resolves every output beneath only `--output-root`; reject any repository compatibility output field before creating a directory or file. `render-compat` first renders canonical views beneath its output root, then writes only allowlisted new paths beneath its explicit repository root. Validate all inputs and both output-containment sets before any write. Keep diff rendering available for Task 2 with exact revision/hash fields.</action>
  <verify>
    <automated>tmp="$(mktemp -d)" &amp;&amp; out="$tmp/default" &amp;&amp; python3 scripts/generate-capability-governance.py render --manifest docs/agent-program/kiana-completion/governance/current.json --output-root "$out" &amp;&amp; cp -a "$out" "$tmp/default-first" &amp;&amp; python3 scripts/generate-capability-governance.py render --manifest docs/agent-program/kiana-completion/governance/current.json --output-root "$out" &amp;&amp; diff -ru "$tmp/default-first" "$out" &amp;&amp; probe="$(mktemp -d)" &amp;&amp; python3 -c 'import json,pathlib,sys; src=pathlib.Path(sys.argv[1]); dst=pathlib.Path(sys.argv[2]); d=json.loads(src.read_text()); d["repository_output_path"]=sys.argv[3]; dst.write_text(json.dumps(d,sort_keys=True)+"\n")' docs/agent-program/kiana-completion/governance/current.json "$probe/attempt.json" "$probe/escape.txt" &amp;&amp; if python3 scripts/generate-capability-governance.py render --manifest "$probe/attempt.json" --output-root "$probe/out"; then exit 1; else rc=$?; test "$rc" -eq 1; fi &amp;&amp; python3 -c 'import pathlib,sys; root=pathlib.Path(sys.argv[1]); out=(root/"out").resolve(); entries={p.relative_to(root).as_posix() for p in root.rglob("*") if not (p.resolve()==out or out in p.resolve().parents)}; assert entries=={"attempt.json"},entries; assert not (root/"escape.txt").exists()' "$probe" &amp;&amp; compat="$tmp/compat" &amp;&amp; repo="$tmp/repository" &amp;&amp; python3 scripts/generate-capability-governance.py render-compat --manifest docs/agent-program/kiana-completion/governance/current.json --output-root "$compat" --repository-output-root "$repo" --compat-manifest docs/agent-program/kiana-completion/governance/compat-outputs.json &amp;&amp; python3 -c 'import pathlib,sys; root=pathlib.Path(sys.argv[1]); got={p.relative_to(root).as_posix() for p in root.rglob("*") if p.is_file()}; assert got=={"docs/reference_audit/CAPABILITY-GOVERNANCE.generated.txt"},got' "$repo" &amp;&amp; cp -a "$compat" "$tmp/compat-first" &amp;&amp; cp -a "$repo" "$tmp/repository-first" &amp;&amp; python3 scripts/generate-capability-governance.py render-compat --manifest docs/agent-program/kiana-completion/governance/current.json --output-root "$compat" --repository-output-root "$repo" --compat-manifest docs/agent-program/kiana-completion/governance/compat-outputs.json &amp;&amp; diff -ru "$tmp/compat-first" "$compat" &amp;&amp; diff -ru "$tmp/repository-first" "$repo" &amp;&amp; python3 -c 'import hashlib,json,pathlib,sys; print(json.dumps([[p.relative_to(pathlib.Path(r)).as_posix(),hashlib.sha256(p.read_bytes()).hexdigest(),p.stat().st_mtime_ns] for r in sys.argv[1:] for p in sorted(pathlib.Path(r).rglob("*")) if p.is_file()]))' "$compat" "$repo" &gt;"$tmp/check-before.json" &amp;&amp; python3 scripts/generate-capability-governance.py render-compat --manifest docs/agent-program/kiana-completion/governance/current.json --output-root "$compat" --repository-output-root "$repo" --compat-manifest docs/agent-program/kiana-completion/governance/compat-outputs.json --check &amp;&amp; python3 -c 'import hashlib,json,pathlib,sys; print(json.dumps([[p.relative_to(pathlib.Path(r)).as_posix(),hashlib.sha256(p.read_bytes()).hexdigest(),p.stat().st_mtime_ns] for r in sys.argv[1:] for p in sorted(pathlib.Path(r).rglob("*")) if p.is_file()]))' "$compat" "$repo" &gt;"$tmp/check-after.json" &amp;&amp; cmp "$tmp/check-before.json" "$tmp/check-after.json"</automated>
  </verify>
  <acceptance_criteria>Default render is directly executed and byte-repeatable; the repository-path attempt exits 1 with no outside write; separate two-root compat rendering writes only allowlisted paths; `--check` preserves bytes and mtimes.</acceptance_criteria>
  <done>The renderer is a deterministic compiler consumer with executable root confinement, not a fifth authority or a fixture-only claim.</done>
  <commit>git add -- scripts/capability_governance.py scripts/generate-capability-governance.py &amp;&amp; git commit -m "feat(01-11): add deterministic governance renderer"</commit>
</task>

<task type="auto">
  <name>Task 2: Directly generate and tamper-check production ledgers and diffs</name>
  <files>docs/agent-program/kiana-completion/governance/generated/public-parity.md, docs/agent-program/kiana-completion/governance/generated/reference-governance.md, docs/agent-program/kiana-completion/governance/generated/legacy-authority.md, docs/agent-program/kiana-completion/governance/diffs/public-baseline/cc-public-2026-07-15.genesis-to-current.json, docs/agent-program/kiana-completion/governance/diffs/repository-registry/references-2026-07-15.genesis-to-current.json</files>
  <read_first>docs/agent-program/kiana-completion/governance/current.json, docs/agent-program/kiana-completion/governance/public-baselines/cc-public-2026-07-15.json, docs/agent-program/kiana-completion/governance/repository-registry/references-2026-07-15.json, docs/agent-program/kiana-completion/governance/legacy-authority/legacy-authority-2026-07-15.json</read_first>
  <behavior>
    - Public/reference/legacy views expose all required rows, axes, blockers, fingerprints, and replacement facts from explicit current heads.
    - Direct `diff` CLI runs for both `public-baseline` and `repository-registry` repeat byte-for-byte, pass schema validation, and bind exact from/to revision IDs plus SHA-256 of the supplied files.
    - After exactly one output byte is changed independently in each family output, each matching `diff --check` exits 1 and leaves that family's tampered bytes unchanged.
  </behavior>
  <action>Render the three canonical production views from `current.json`. Directly generate public and registry diffs from their exact genesis files to reviewed heads; never validate only a pre-existing JSON artifact. For each family run the CLI twice, compare bytes, run `--check`, validate the diff schema, and independently compute the source-file SHA-256 and revision IDs to compare against the exact fields in `<interfaces>`. Then independently append one byte to the public diff and to the repository-registry diff. For each tampered family run its matching `diff --check`, require exit 1, and prove that exact file remains byte-for-byte tampered rather than repaired or rewritten. Public view must make unmapped/missing/excluded official-source status visible and use strict required-child blockers. Reference view must show HEAD/tree/license/content-only fingerprints and never equate Adopt/Adapt with implementation. Legacy view lists every D-23 entry and clearly marks old inputs non-authoritative. All files use canonical timestamps/IDs and exact generated markers; no wall clock, filesystem ordering, dirty legacy text, or stored optimistic summary enters output.</action>
  <verify>
    <automated>tmp="$(mktemp -d)" &amp;&amp; pf=docs/agent-program/kiana-completion/governance/public-baselines/cc-public-2026-07-15-genesis.json &amp;&amp; pt=docs/agent-program/kiana-completion/governance/public-baselines/cc-public-2026-07-15.json &amp;&amp; rf=docs/agent-program/kiana-completion/governance/repository-registry/references-2026-07-15-genesis.json &amp;&amp; rt=docs/agent-program/kiana-completion/governance/repository-registry/references-2026-07-15.json &amp;&amp; po="$tmp/public.json" &amp;&amp; ro="$tmp/repository.json" &amp;&amp; python3 scripts/generate-capability-governance.py diff --family public-baseline --from "$pf" --to "$pt" --output "$po" &amp;&amp; cp "$po" "$tmp/public-first.json" &amp;&amp; python3 scripts/generate-capability-governance.py diff --family public-baseline --from "$pf" --to "$pt" --output "$po" &amp;&amp; cmp "$tmp/public-first.json" "$po" &amp;&amp; python3 scripts/generate-capability-governance.py diff --family public-baseline --from "$pf" --to "$pt" --output "$po" --check &amp;&amp; python3 scripts/generate-capability-governance.py diff --family repository-registry --from "$rf" --to "$rt" --output "$ro" &amp;&amp; cp "$ro" "$tmp/repository-first.json" &amp;&amp; python3 scripts/generate-capability-governance.py diff --family repository-registry --from "$rf" --to "$rt" --output "$ro" &amp;&amp; cmp "$tmp/repository-first.json" "$ro" &amp;&amp; python3 scripts/generate-capability-governance.py diff --family repository-registry --from "$rf" --to "$rt" --output "$ro" --check &amp;&amp; python3 scripts/validate-json-schema.py docs/schemas/kiana-capability-governance-diff.v1.schema.json "$po" &gt;/dev/null &amp;&amp; python3 scripts/validate-json-schema.py docs/schemas/kiana-capability-governance-diff.v1.schema.json "$ro" &gt;/dev/null &amp;&amp; python3 -c 'import hashlib,json,pathlib,sys; pairs=[(sys.argv[1],sys.argv[2],sys.argv[3]),(sys.argv[4],sys.argv[5],sys.argv[6])]; [(lambda d,f,t: (_ for _ in ()).throw(AssertionError(d)) if (d["from_revision_id"]!=json.loads(f.read_text())["revision_id"] or d["to_revision_id"]!=json.loads(t.read_text())["revision_id"] or d["from_revision_sha256"]!=hashlib.sha256(f.read_bytes()).hexdigest() or d["to_revision_sha256"]!=hashlib.sha256(t.read_bytes()).hexdigest()) else None)(json.loads(pathlib.Path(o).read_text()),pathlib.Path(f),pathlib.Path(t)) for o,f,t in pairs]' "$po" "$pf" "$pt" "$ro" "$rf" "$rt" &amp;&amp; python3 -c 'import pathlib,sys; p=pathlib.Path(sys.argv[1]); p.write_bytes(p.read_bytes()+b" ")' "$po" &amp;&amp; cp "$po" "$tmp/public-tampered.json" &amp;&amp; if python3 scripts/generate-capability-governance.py diff --family public-baseline --from "$pf" --to "$pt" --output "$po" --check; then exit 1; else rc=$?; test "$rc" -eq 1; fi &amp;&amp; cmp "$tmp/public-tampered.json" "$po" &amp;&amp; python3 -c 'import pathlib,sys; p=pathlib.Path(sys.argv[1]); p.write_bytes(p.read_bytes()+b" ")' "$ro" &amp;&amp; cp "$ro" "$tmp/repository-tampered.json" &amp;&amp; if python3 scripts/generate-capability-governance.py diff --family repository-registry --from "$rf" --to "$rt" --output "$ro" --check; then exit 1; else rc=$?; test "$rc" -eq 1; fi &amp;&amp; cmp "$tmp/repository-tampered.json" "$ro" &amp;&amp; python3 scripts/generate-capability-governance.py render --manifest docs/agent-program/kiana-completion/governance/current.json --output-root docs/agent-program/kiana-completion/governance/generated --check</automated>
  </verify>
  <acceptance_criteria>Both family diffs are produced by real CLI calls twice plus `--check`, validate structurally, bind independently recomputed exact IDs/hashes, and each family's independent one-byte mutation causes exit 1 without rewrite; production views remain exact-byte reproducible and visibly generated.</acceptance_criteria>
  <done>Users can inspect COD-01, DIF-11, proof honesty, revision changes, and legacy replacement from deterministic views whose CLI behavior is directly proven.</done>
  <commit>git add -- docs/agent-program/kiana-completion/governance/generated/public-parity.md docs/agent-program/kiana-completion/governance/generated/reference-governance.md docs/agent-program/kiana-completion/governance/generated/legacy-authority.md docs/agent-program/kiana-completion/governance/diffs/public-baseline/cc-public-2026-07-15.genesis-to-current.json docs/agent-program/kiana-completion/governance/diffs/repository-registry/references-2026-07-15.genesis-to-current.json &amp;&amp; git commit -m "docs(01-11): generate governance ledgers"</commit>
</task>

<task type="auto">
  <name>Task 3: Generate directory warning and finish all progressive runner slices</name>
  <files>scripts/capability-governance-smoke.sh, docs/reference_audit/CAPABILITY-GOVERNANCE.generated.txt</files>
  <read_first>scripts/capability-governance-smoke.sh, docs/agent-program/kiana-completion/governance/compat-outputs.json, docs/agent-program/kiana-completion/governance/current.json, .planning/phases/01-baseline-evidence-governance/01-VALIDATION.md</read_first>
  <behavior>
    - Compatibility rendering writes the new directory warning/index only when both repository root and allowlist manifest are explicit.
    - `generated-views` repeats the real Task 1/2 confinement, two-family diff, exact binding, and separate public-baseline plus repository-registry one-byte tamper/no-rewrite contracts under temporary roots.
    - `semantic-negative` and `production` explicitly exercise `evidence_expired` and `newer_failed_retest` against manifest `evaluation_time`, asserting exact code, subject, `status=invalid`, and `freshness=stale` while preserving older history.
    - No-argument runner invokes every progressively available slice in dependency order; `production` traverses every selected family head to genesis and checks source, 38 identities, all drift, legacy hashes, exact generated bytes, and safe roots offline.
  </behavior>
  <action>Run `render-compat` with production output root, repository root `.`, and the checked-in allowlist to create the generated text warning linking to canonical public/reference/legacy views and stating that audit Markdown files are frozen inputs. Extend the focused runner with only `generated-views` and `production`, preserving all earlier slices. `generated-views` directly repeats default-render confinement, separate two-root allowlisted compatibility, byte/mtime no-rewrite, both family diff generation, exact from/to ID/hash validation, and independent one-byte tamper/exit-1/no-rewrite checks for both public-baseline and repository-registry outputs. `production` validates explicit current heads and their RFC3339 `evaluation_time`, full ancestry including non-genesis predecessors for every family, live 38 fingerprints, controlled official artifact, target bindings, legacy inventory/hash, production render/diff checks, and elapsed time. Both `semantic-negative` and the production path must run the independent `evidence_expired` and `newer_failed_retest` cases and assert their exact code, subject, invalid status, stale freshness, and retained older record. No argument must invoke `schemas`, `fixture-shapes`, `public-baseline`, `reference-governance`, `semantic-negative`, `drift-refresh`, `legacy-authority`, `generated-views`, and `production` in that order. Keep the gate offline and below 30 seconds. Do not edit the dirty `scripts/schema-contract-smoke.sh` in this plan; Plan 01-12 depends on this plan and owns the required one-line broad-gate integration through a preservation-proven alternate Git index.</action>
  <verify>
    <automated>python3 scripts/generate-capability-governance.py render-compat --manifest docs/agent-program/kiana-completion/governance/current.json --output-root docs/agent-program/kiana-completion/governance/generated --repository-output-root . --compat-manifest docs/agent-program/kiana-completion/governance/compat-outputs.json --check &amp;&amp; bash scripts/capability-governance-smoke.sh semantic-negative &amp;&amp; bash scripts/capability-governance-smoke.sh generated-views &amp;&amp; bash scripts/capability-governance-smoke.sh &amp;&amp; bash scripts/capability-governance-smoke.sh production</automated>
  </verify>
  <acceptance_criteria>No-argument and explicit production runs pass below 30 seconds offline and invoke all progressive slices; both D-17 cases return exact subject/status/freshness; both diff families fail tamper checks without rewrite; production cannot omit predecessors; any source/identity/history/hash/view/diff/legacy/output-root drift fails; existing dirty files remain unstaged and byte-unchanged.</acceptance_criteria>
  <done>Phase 1 has a self-contained aggregate and production gate plus visible legacy warning, ready for Plan 01-12 to wire exactly once into the dirty broad repository gate without absorbing user changes.</done>
  <commit>git add -- scripts/capability-governance-smoke.sh docs/reference_audit/CAPABILITY-GOVERNANCE.generated.txt &amp;&amp; git commit -m "test(01-11): gate governance production state"</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated canonical strings -> Markdown/text | Source titles, rationales, paths, and URLs can inject markup or disclose local data. |
| Canonical heads -> generated bytes/diffs | Nondeterminism, wrong from/to binding, or hand edits can falsify visible status. |
| Default/compat output roots -> repository paths | A manifest or allowlist can escape temporary roots and overwrite real files. |
| Focused gate -> production handoff | Omitted predecessor/source/drift/legacy checks can certify incomplete governance. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-01 | Tampering/Disclosure | renderer | high | mitigate | Validate-first, context-aware escaping, omit secrets/absolute paths, hostile fixture. |
| T-11-02 | Tampering/Repudiation | generated views/diffs | high | mitigate | Canonical time/order, exact-byte checks, independently recomputed from/to bindings, tamper no-rewrite. |
| T-11-03 | Elevation/Tampering | output roots | high | mitigate | Default-root rejection, separate compat command, explicit two roots, allowlist, containment preflight. |
| T-11-04 | Spoofing | production gate | high | mitigate | Explicit heads, all predecessor traversal, source/38/drift/legacy/view/diff checks. |
| T-11-SC | Tampering | dependency supply chain | low | accept | No package or dependency change; standard library and existing helper only. |
</threat_model>

<verification>
Run all three task commands, no-argument and production runner modes, `python3 -m py_compile scripts/capability_governance.py scripts/generate-capability-governance.py`, and `git diff --check --` over the nine exact plan paths. Confirm exact staged paths contain only new Phase 1 artifacts and never broad legacy/script paths.
</verification>

<success_criteria>
- Default render directly proves zero writes outside its output root; compat rendering requires two explicit roots and an allowlist.
- Both production diffs are generated by direct CLI calls, exact-ID/hash-bound, repeatable, and tamper-sensitive without rewrite.
- Production views expose all blockers/axes; expiry and newer failed retest are exact-code current-completion failures; all progressive slices run through the aggregate runner.
- Dirty legacy docs remain untouched, and broad schema smoke remains untouched in this plan for the mandatory isolated integration in Plan 01-12.
</success_criteria>

## Artifacts this phase produces

- Shared render/diff functions and thin `generate-capability-governance.py` CLI.
- Generated public, reference, and legacy authority views plus two genesis-to-current diffs.
- New directory warning/index `docs/reference_audit/CAPABILITY-GOVERNANCE.generated.txt`.
- Final runner slices `generated-views` and `production`, plus no-argument aggregate dispatch.

## Source Coverage Audit

| SOURCE | ID | Feature / constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | Phase 1 | Frozen public/reference facts and proof honesty | 01-08, 01-09, 01-10, 01-11, 01-12 | COVERED |
| REQ | COD-01 | Source-complete public parity ledger with evidence/difference per row | 01-01, 01-03, 01-04, 01-06, 01-08, 01-10, 01-11, 01-12 | COVERED |
| REQ | DIF-11 | 38 audited identities and capability decisions with license/security/owner/test/evidence | 01-01, 01-03, 01-05, 01-06, 01-09, 01-10, 01-11, 01-12 | COVERED |
| RESEARCH | Immutable snapshot directories and mandatory ancestry | 01-01, 01-02, 01-05, 01-06, 01-07, 01-08, 01-09, 01-10 | COVERED |
| RESEARCH | Shared Python module plus thin CLIs; no Rust adapter/package | 01-06, 01-07, 01-11 | COVERED |
| RESEARCH | Controlled normalized official artifact with archive identity and offline fixture | 01-01, 01-03, 01-07, 01-08 | COVERED |
| RESEARCH | Content-only normalized tree SHA-256 | 01-01, 01-03, 01-05, 01-06, 01-07, 01-09 | COVERED |
| RESEARCH | Per-path legacy classification plus generated replacement index | 01-02, 01-10, 01-11 | COVERED |
| RESEARCH | Focused governance runner is invoked exactly once by the existing broad schema contract gate | 01-12 | COVERED |
| CONTEXT | D-01..D-06 | Public journey/source/snapshot/completion contract | 01-01, 01-03, 01-04, 01-06, 01-08, 01-11 | COVERED |
| CONTEXT | D-07..D-12 | 38-repo registry, aliases, decisions, governance/product separation | 01-01, 01-03, 01-05, 01-06, 01-09, 01-11 | COVERED |
| CONTEXT | D-13..D-18 | Coverage/proof/freshness/evidence and drift semantics | 01-02, 01-03, 01-05, 01-06, 01-07, 01-10, 01-11 | COVERED |
| CONTEXT | D-19..D-24 | JSON authority, fail-closed validation, deterministic views, migration, existing patterns, and D-24 broad integration | 01-01, 01-02, 01-03, 01-05, 01-06, 01-07, 01-10, 01-11, 01-12 | COVERED |

Deferred ideas remain excluded: capability repair routes to later roadmap phases; continuous monitoring is not built; target/cloud/enterprise/user-value proof is not synthesized.

Plan `01-12` is the required final dependency after this plan; it owns broad schema-smoke wiring and may not be skipped or deferred.

<output>
Create `.planning/phases/01-baseline-evidence-governance/01-11-SUMMARY.md` when done.
</output>
