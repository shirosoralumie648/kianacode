---
phase: 01-baseline-evidence-governance
plan: "04"
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json
  - scripts/fixtures/capability-governance/invalid/coverage/missing-required-capability.json
  - scripts/fixtures/capability-governance/invalid/coverage/unmapped-source-entry.json
  - scripts/fixtures/capability-governance/invalid/coverage/duplicate-source-mapping.json
  - scripts/fixtures/capability-governance/invalid/coverage/unjustified-exclusion.json
autonomous: true
requirements:
  - COD-01
  - DIF-11
must_haves:
  truths:
    - "D-01/D-02/COD-01: official source entries and required capability IDs have a complete bidirectional negative oracle, not only a 15-journey count."
    - "D-03..D-06/D-21: missing, duplicate, or unjustifiably excluded public behavior fails for its own stable reason."
  artifacts:
    - path: scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json
      provides: Exact coverage-case to diagnostic mapping
    - path: scripts/fixtures/capability-governance/invalid/coverage/missing-required-capability.json
      provides: Required child absent from canonical capability rows
    - path: scripts/fixtures/capability-governance/invalid/coverage/unmapped-source-entry.json
      provides: Official artifact entry absent from the public source index
  key_links:
    - from: scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json
      to: docs/schemas/kiana-public-parity-baseline.v1.schema.json
      via: one mutation per official-source coverage invariant
      pattern: expected_code
---

<objective>
独立编码 COD-01 official-source completeness RED corpus，确保遗漏公开子能力或来源条目时验证不会因 15 个 journey 名称存在而全绿。

Purpose: 直接关闭 P1-B01，并为 D-01..D-06/D-21 的 semantic validator 提供稳定错误 oracle。
Output: 一个 manifest 和四个单一不变量的 invalid fixtures。
</objective>

<execution_context>
@/home/shirosora/.codex/gsd-core/workflows/execute-plan.md
@/home/shirosora/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-baseline-evidence-governance/01-01-SUMMARY.md
@.planning/phases/01-baseline-evidence-governance/01-CONTEXT.md
@.planning/phases/01-baseline-evidence-governance/01-VALIDATION.md
@docs/schemas/kiana-public-parity-baseline.v1.schema.json

<interfaces>
Stable codes are exactly `missing_required_capability`, `unmapped_source_entry`, `duplicate_source_mapping`, and `unjustified_exclusion`.
Each fixture starts from the same valid public/source graph and mutates exactly one invariant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode absent required capability and unmapped official entry</name>
  <files>scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json, scripts/fixtures/capability-governance/invalid/coverage/missing-required-capability.json, scripts/fixtures/capability-governance/invalid/coverage/unmapped-source-entry.json</files>
  <read_first>scripts/fixtures/capability-governance/valid/offline-source-identity.json, docs/schemas/kiana-official-source-artifact.v1.schema.json, docs/schemas/kiana-public-parity-baseline.v1.schema.json, .planning/phases/01-baseline-evidence-governance/01-CONTEXT.md</read_first>
  <behavior>
    - Removing a required capability row while its journey/source mapping remains produces only `missing_required_capability`.
    - Removing one official artifact entry from `official_source_index` produces only `unmapped_source_entry`.
    - The manifest names fixture, expected code, subject ID, and mutated JSON path.
  </behavior>
  <action>Create the manifest and two fixtures by copying the positive offline-source identity graph and changing exactly the named relationship. Preserve all 15 journey IDs and all unrelated valid records so a generic count or unrelated schema failure cannot satisfy the test. The absent capability case retains the journey required ID and source mapping; the unmapped-entry case retains the artifact entry while removing its only public mapping/exclusion.</action>
  <verify>
    <automated>python3 -m json.tool scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json &gt;/dev/null &amp;&amp; python3 -m json.tool scripts/fixtures/capability-governance/invalid/coverage/missing-required-capability.json &gt;/dev/null &amp;&amp; python3 -m json.tool scripts/fixtures/capability-governance/invalid/coverage/unmapped-source-entry.json &gt;/dev/null &amp;&amp; python3 -c 'import json; from pathlib import Path; m=json.loads(Path("scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json").read_text()); assert {x["expected_code"] for x in m["cases"]} &gt;= {"missing_required_capability","unmapped_source_entry"}'</automated>
  </verify>
  <acceptance_criteria>Both fixtures are structurally readable, preserve unrelated validity, and have one exact manifest diagnostic with subject/path.</acceptance_criteria>
  <done>The validator can be driven RED for missing required behavior and unmapped official source entries independently.</done>
  <commit>git add -- scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json scripts/fixtures/capability-governance/invalid/coverage/missing-required-capability.json scripts/fixtures/capability-governance/invalid/coverage/unmapped-source-entry.json &amp;&amp; git commit -m "test(01-04): add missing public coverage cases"</commit>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode duplicate mapping and unjustified exclusion</name>
  <files>scripts/fixtures/capability-governance/invalid/coverage/duplicate-source-mapping.json, scripts/fixtures/capability-governance/invalid/coverage/unjustified-exclusion.json</files>
  <read_first>scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json, scripts/fixtures/capability-governance/valid/offline-source-identity.json, docs/schemas/kiana-public-parity-baseline.v1.schema.json, .planning/phases/01-baseline-evidence-governance/01-RESEARCH.md</read_first>
  <behavior>
    - Mapping one source entry twice or repeating a capability ID produces only `duplicate_source_mapping`.
    - An exclusion missing classification, rationale, review evidence, or review revision produces only `unjustified_exclusion`.
    - Exclusion never removes a required capability already promised by a journey.
  </behavior>
  <action>Create two focused mutations and append their exact cases to the manifest from Task 1. Exercise duplicate record and duplicate capability ID forms in the duplicate fixture's case list. Exercise each required exclusion field through declarative mutations in the exclusion fixture while keeping source artifact identity valid. Include an assertion case that a reviewed exclusion cannot mask a journey-required capability.</action>
  <verify>
    <automated>python3 -m json.tool scripts/fixtures/capability-governance/invalid/coverage/duplicate-source-mapping.json &gt;/dev/null &amp;&amp; python3 -m json.tool scripts/fixtures/capability-governance/invalid/coverage/unjustified-exclusion.json &gt;/dev/null &amp;&amp; python3 -c 'import json; from pathlib import Path; p=Path("scripts/fixtures/capability-governance/invalid/coverage"); m=json.loads((p/"expected-errors.json").read_text()); assert {x["expected_code"] for x in m["cases"]} == {"missing_required_capability","unmapped_source_entry","duplicate_source_mapping","unjustified_exclusion"}; assert all((p/x["fixture"]).is_file() for x in m["cases"])'</automated>
  </verify>
  <acceptance_criteria>The manifest has all four exact codes and every referenced fixture; duplicate/exclusion cases preserve unrelated source, journey, and evidence validity.</acceptance_criteria>
  <done>The public-baseline family has a complete official-source mapping/exclusion RED oracle.</done>
  <commit>git add -- scripts/fixtures/capability-governance/invalid/coverage/expected-errors.json scripts/fixtures/capability-governance/invalid/coverage/duplicate-source-mapping.json scripts/fixtures/capability-governance/invalid/coverage/unjustified-exclusion.json &amp;&amp; git commit -m "test(01-04): complete public coverage oracle"</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Official source artifact -> source index | Omitted or duplicated entries can hide published behavior. |
| Exclusion review -> capability coverage | A weak exclusion can relabel a missing required behavior as out of scope. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Tampering | source mapping | high | mitigate | Bidirectional entry/capability oracle and exact duplicate detection. |
| T-04-02 | Repudiation | exclusion | high | mitigate | Classification, rationale, evidence IDs, and review revision are all mandatory. |
| T-04-03 | Spoofing | journey completeness | high | mitigate | Required IDs remain explicit and cannot be erased by an exclusion. |
</threat_model>

<verification>
Run both task commands and `git diff --check -- scripts/fixtures/capability-governance/invalid/coverage`.
</verification>

<success_criteria>
- All four checker-required source coverage errors have focused fixtures.
- A valid 15-journey shell cannot hide absent required capabilities or source entries.
- Source mapping remains inside the public-baseline family.
- No implementation, network, package, or dirty legacy file is touched.
</success_criteria>

## Artifacts this phase produces

- Coverage error manifest and four focused invalid fixtures.
- Stable errors: `missing_required_capability`, `unmapped_source_entry`, `duplicate_source_mapping`, `unjustified_exclusion`.

<output>
Create `.planning/phases/01-baseline-evidence-governance/01-04-SUMMARY.md` when done.
</output>
