---
phase: 02-reproducible-toolchain-dependency-convergence
plan: "02"
type: execute
wave: 2
depends_on:
  - 02-01
reconstructed: true
reconstructed_at: "2026-08-22"
reconstructed_from:
  - .planning/phases/02-reproducible-toolchain-dependency-convergence/02-02-SUMMARY.md
  - 0eaaa9e
  - 05cc81a
already_landed: true
files_modified:
  - .github/workflows/release-smoke.yml
  - scripts/commercial-release-blockers-report.sh
  - docs/schemas/kiana-license-compliance-summary.v1.schema.json
  - scripts/generate-license-summary.sh
  - scripts/sign-sbom.sh
autonomous: true
requirements:
  - DIF-12
must_haves:
  truths:
    - "D-06: release-smoke.yml records dist/build-inputs.json after cargo fetch --locked using the live rustc version, Cargo.lock SHA-256, and Git HEAD."
    - "D-08..D-10: commercial-release-blockers-report.sh supports --md and --audience=maintainer|user, and missing toolchain/SBOM/license artifacts stay blocking instead of being marked complete."
    - "D-12..D-13: sign-sbom.sh can emit a redacted user SBOM and optional signature, and generate-license-summary.sh emits kiana.license-compliance-summary.v1."
  artifacts:
    - path: .github/workflows/release-smoke.yml
      provides: Record build inputs step writing dist/build-inputs.json
    - path: scripts/commercial-release-blockers-report.sh
      provides: toolchain/SBOM/license checks plus --md dual-audience output
    - path: scripts/sign-sbom.sh
      provides: User-redacted SBOM export and optional KIANA_SIGNING_COMMAND signature
    - path: scripts/generate-license-summary.sh
      provides: Standalone JSON/Markdown license compliance summary
    - path: docs/schemas/kiana-license-compliance-summary.v1.schema.json
      provides: Closed license-summary contract
  key_links:
    - from: .github/workflows/release-smoke.yml
      to: docs/schemas/kiana-build-inputs.v1.schema.json
      via: CI records schema=kiana.build-inputs.v1
      pattern: kiana.build-inputs.v1
    - from: scripts/commercial-release-blockers-report.sh
      to: rust-toolchain.toml
      via: toolchain.rust-toolchain-file check
      pattern: toolchain.rust-toolchain-file
    - from: scripts/commercial-release-blockers-report.sh
      to: scripts/sign-sbom.sh
      via: sbom.signed remains blocking until a signature file exists
      pattern: sbom.signed
    - from: scripts/generate-license-summary.sh
      to: docs/schemas/kiana-license-compliance-summary.v1.schema.json
      via: schema kiana.license-compliance-summary.v1
      pattern: kiana.license-compliance-summary.v1
---

> **Reconstruction note (2026-08-22):** Original `02-02-PLAN.md` (and the contemplated `02-03-PLAN.md`) were lost with the unpushed `feature/tui-mega-upgrade` branch. This file is a retroactive record of Waves 2 and 3 as actually landed in `0eaaa9e` and `05cc81a` / `02-02-SUMMARY.md`. Wave 3 was not given a separate live PLAN/SUMMARY pair. This is not a request to re-implement those waves.

<objective>
Make build inputs, blocker visibility, SBOM user export, and license summary inspectable (Phase 2 Waves 2-3).

Purpose: Maintainers can record reproducible build inputs in CI; users/admins can export a redacted readiness view; missing CI/SBOM/signing/license artifacts stay blocking.
Output: CI record-build-inputs step, five new blocker checks, `--md` dual-audience output, `sign-sbom.sh`, `generate-license-summary.sh`, and the license-summary schema.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-reproducible-toolchain-dependency-convergence/02-CONTEXT.md
@.planning/phases/02-reproducible-toolchain-dependency-convergence/02-01-SUMMARY.md
@.planning/phases/02-reproducible-toolchain-dependency-convergence/02-02-SUMMARY.md
@scripts/commercial-release-blockers-report.sh

<interfaces>
- dist/ artifacts are generated, not committed (`dist/` is gitignored).
- toolchain.build-inputs, sbom.present, sbom.signed, and license.compliance-summary are allowed to remain blocking until real CI/signing/build artifacts exist.
- D-11 full release-signature.json / Sigstore chain stays outside this plan; Wave 3 only adds the SBOM signing hook and user-redacted SBOM.
- User `--md --audience=user` output is category-level and must not leak host paths.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CI Record build inputs step</name>
  <files>.github/workflows/release-smoke.yml</files>
  <read_first>docs/schemas/kiana-build-inputs.v1.schema.json</read_first>
  <behavior>
    - After cargo fetch --locked, write dist/build-inputs.json with kiana.build-inputs.v1
    - Capture rustc --version field 2, sha256 of Cargo.lock, and git rev-parse HEAD
  </behavior>
  <action>Historical: landed in 0eaaa9e. A later verification fix must make the Python step read the shell-exported TOOLCHAIN_VERSION / CARGO_LOCK_HASH / SOURCE_HASH rather than empty GitHub env.toolchain_version mappings.</action>
  <verify>
    <automated>rg -n "Record build inputs|kiana.build-inputs.v1" .github/workflows/release-smoke.yml</automated>
  </verify>
  <done>release-smoke.yml contains the Record build inputs step</done>
</task>

<task type="auto">
  <name>Task 2: Blocker report toolchain/SBOM presence checks</name>
  <files>scripts/commercial-release-blockers-report.sh</files>
  <read_first>docs/schemas/kiana-commercial-release-blockers.v1.schema.json</read_first>
  <behavior>
    - toolchain.rust-toolchain-file, toolchain.build-inputs, sbom.present are first-class checks
    - Missing generated dist artifacts stay blocking
  </behavior>
  <action>Historical: landed in 0eaaa9e. Do not recreate.</action>
  <verify>
    <automated>rg -n "toolchain.rust-toolchain-file|toolchain.build-inputs|sbom.present" scripts/commercial-release-blockers-report.sh</automated>
  </verify>
  <done>The three Wave 2 checks exist in the blocker report</done>
</task>

<task type="auto">
  <name>Task 3: --md dual-audience output</name>
  <files>scripts/commercial-release-blockers-report.sh</files>
  <read_first>.planning/phases/02-reproducible-toolchain-dependency-convergence/02-CONTEXT.md</read_first>
  <behavior>
    - --md defaults to maintainer handoff markdown
    - --md --audience=user renders category counts without paths/hostnames
  </behavior>
  <action>Historical: landed in 0eaaa9e. Do not recreate.</action>
  <verify>
    <automated>bash scripts/commercial-release-blockers-report.sh --md --audience=user</automated>
  </verify>
  <done>User markdown audience renders without crashing</done>
</task>

<task type="auto">
  <name>Task 4: sign-sbom.sh user export and optional signature</name>
  <files>scripts/sign-sbom.sh</files>
  <read_first>scripts/generate-sbom.sh</read_first>
  <behavior>
    - Redact path+file:// bom-refs to name@version and drop file:// externalReferences
    - If KIANA_SIGNING_COMMAND is unset, skip signing with a clear message and exit 0
  </behavior>
  <action>Historical: landed in 05cc81a. Do not recreate.</action>
  <verify>
    <automated>test -x scripts/sign-sbom.sh && rg -n "KIANA_SIGNING_COMMAND|sbom.cdx.user.json" scripts/sign-sbom.sh</automated>
  </verify>
  <done>sign-sbom.sh exists and documents the unsigned skip path</done>
</task>

<task type="auto">
  <name>Task 5: generate-license-summary.sh + schema</name>
  <files>scripts/generate-license-summary.sh, docs/schemas/kiana-license-compliance-summary.v1.schema.json</files>
  <read_first>docs/schemas/kiana-build-inputs.v1.schema.json</read_first>
  <behavior>
    - cargo metadata --locked --offline
    - schema kiana.license-compliance-summary.v1 with --json/--md/--out/--md-out
  </behavior>
  <action>Historical: landed in 05cc81a. Do not recreate.</action>
  <verify>
    <automated>test -x scripts/generate-license-summary.sh && test -f docs/schemas/kiana-license-compliance-summary.v1.schema.json</automated>
  </verify>
  <done>License summary script and schema exist</done>
</task>

<task type="auto">
  <name>Task 6: sbom.signed and license.compliance-summary blocker checks</name>
  <files>scripts/commercial-release-blockers-report.sh</files>
  <read_first>scripts/sign-sbom.sh, scripts/generate-license-summary.sh</read_first>
  <behavior>
    - New checks remain blocking until dist/sbom.cdx.json.sig and dist/license-summary.json exist
    - Total blocker checks become 22
  </behavior>
  <action>Historical: landed in 05cc81a. Do not recreate.</action>
  <verify>
    <automated>rg -n "sbom.signed|license.compliance-summary" scripts/commercial-release-blockers-report.sh</automated>
  </verify>
  <done>Wave 3 checks exist and are designed to block without generated artifacts</done>
</task>

</tasks>
