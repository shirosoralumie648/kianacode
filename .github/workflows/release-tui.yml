# Retired: kiana-tui and kiana-components were archived under legacy/ and are
# excluded from the workspace, so there is no TUI binary to build or release.
name: Release TUI Binaries (retired)

on:
  workflow_dispatch:

permissions: {}

jobs:
  retired:
    name: TUI release pipeline retired
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: echo "kiana-tui was archived under legacy/ and is excluded from the workspace."
